# hk-rpc — ORPHAN (removed from the workspace in v0.13.2, H11)

A 27-line DTO stub from 0.7 that nothing depends on; the live JSON-RPC surface is `crates/hk-node/src/rpc.rs` (reference: `docs/RPC.md`). Kept on disk only until the next housekeeping commit deletes the directory.
