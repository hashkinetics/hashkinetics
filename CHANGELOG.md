# HashKinetics — Changelog

All notable changes to this project. Versions are project-level (see `version.md` for per-crate status). Dates are IST-day of work.

## [Unreleased] — 2026-09-10

- **Seat #11 — ATTO, the seventh external operator (2026-09-12 ≈ 10:02 UTC, block 379,143).** A Discord operator (`lol123988 [ATTO]`, node at 43.130.11.0/24 — on the gateway's roll call since the previous day, v0.19.0, genesis match) posted his `validator.json` at 08:07 UTC; the address derived from his public key, `28BD83BC…`, matched what his node printed; his `hk_chainInfo` at height 377,719 gave an app_hash equal to block 377,720's `parent_app_hash` on the public RPC; the four founding seats signed the admission (root `4de79d0d…`, power 1, window 378,299–418,249) and it committed in block 379,143 — the set is 10 → 11, power 23, effective from 379,144. Twenty-nine minutes from his numbers to the commit. **This was the last seat the founding seats can admit on their own:** at 11 seats their 16 of 23 is still more than two-thirds; at 12 it would be 16 of 24, exactly two-thirds, which the rule does not accept — the twelfth seat needs an external co-signature or the bootstrap handover, as designed. Ops note kept: this admission was driven from a workstation rather than Cloud Shell, which surfaced two script faults (a sticky-`/tmp` file owned by another user; a passphrase pipe the seat user could not re-open) — the assemble step refused the stale approvals exactly as intended, nothing was submitted, both faults fixed, second run clean.
- **Incident 2026-09-12 06:29–07:25 UTC — public RPC and prover endpoints unreachable; the chain never paused.** At 06:29 an unattended glibc security update on the gateway host restarted every service linked against it; nginx (which terminates TLS for `rpc.` and `prover.hashkinetics.org`) came back while the host's resolver was itself restarting, could not resolve a hostname in one internal proxy stanza at start-up, and stayed down — the unit had no automatic retry. Every validator kept committing throughout (heights ~370,300 → 371,900 over the window); the p2p port, the seats, the Discord control center and the bridge service were unaffected, so no operator's node saw anything. Fixed at ~07:25 by restarting nginx. Hardening the same hour: nginx now retries on failure every 15 s; the proxy stanza resolves at request time instead of at start-up, so a DNS blip can no longer take down the whole server; and the automatic-restart tool is told never to restart `hk-node` on any of our hosts — the same update had restarted the founding seat that lives on the gateway host at 06:29:27 (one missed proposer slot in the day's 707, no quorum impact), and the other three founding seats' restarts of the previous morning (06:20–06:53 UTC) match the same mechanism. Founding seats now take library updates only at planned rolls; operators on Ubuntu should expect the same from `needrestart` and exclude `hk-node` (a line in the onboarding guide). Lesson kept: an outage of the *window* onto the chain is not an outage of the chain, but the site's live fields going blank read as one — status.json for the seats page is the fix already on the list.
- **The privacy chains, side by side (`docs/PRIVACY-CHAIN-COMPARISON.md` + the home page `#compare`).** Monero, Zcash, Firo, Pirate Chain, Dash, Decred, Secret, Oasis, Horizen, Beldex against HashKinetics on thirty properties — privacy default, what is hidden, primitive, trusted setup, post-quantum status split into spend signatures / validator keys / the privacy layer itself, disclosure and viewing keys, master key, regulated-asset controls, consensus budgets, multi-asset shielding, bridge, proof cost, per-block aggregation, throughput, audits, governance, liquidity. Competitor cells from each project's public documentation (neutral by intent, corrections invited); every HashKinetics cell measured or labelled plan; the audit and liquidity rows say what they have and we do not. The home page carries a 10-row condensed table and four "major differences" cards. Throughput is stated two ways and labelled both ways: 274 tx/s on-chain (4-validator lab chain, measured) and ~180,000 payments/s effective through native PayWord channels (183 settles/s × 1,000 payments per settle — arithmetic on measured constants, never a measured run).
- **`hk-node storm` on a public chain.** `HK_STORM_SENDERS=DIR,DIR,…` signs the load from real account directories (`account-new`, funded) instead of the lab chain's genesis names — the chain's nonce wins on load, fee affordability is printed per sender, and every sender's ratchet index is written back at the end. This is the harness for the 30-minute testnet-1 run (the capacity sheet's M1 row), after which the site's throughput label changes from "lab chain" to "testnet-1, measured".
- Site wording: "devnet" no longer appears on the site — provenance stays exact as "4-validator lab chain" / "local chain".

## [0.19.1] — 2026-09-09 — 🌉 B1: the Sepolia USDC ↔ testnet-1 bridge, LIVE on the testnets both ways · the first shielded USDC.sep · the CLI wallet binds to a real account (client-only: no consensus change, no deadline, no roll needed)

- **Release:** `hk-node-v0.19.1.gz` sha256 `a8e6c78af9f320452881030b6d848869bc463956fa97e1f411591410bb9a8654` · unpacked binary sha256 `a6a4a56ffc0d8339564ccd6392140e4890316cd001f2e04e22bedd168eaaf22d` · `hk-node --version` → `hk-node v0.19.1`. Client-only: a v0.19.0 seat keeps voting and never needs it.
- **The first shielded bridged USDC (2026-09-09 ~17:55–18:00 UTC, testnet-1, the receipt P6 + B1 were built for).** From the demo account that received the bridged USDC.sep: `wallet shield 2 --asset 0c3c3f40…` → txid `5ae652ee3955bdd1a5476100135534c042a5966eb3c30378f01b7d0f954fc7b6` at height 214,097 (mint proof 1,056 ms on the public prover; **`hk_getPools` count 1 → 2** — the USDC.sep pool opened on first use with its own tree, nullifiers and conservation total 2,000,000; transparent 15 → 13) · a **0.5 USDC.sep payment to a second wallet's stealth address, fee 0** — txid `4ddf106e040ce3362296664e81bdeddc68cc8f792642fa8da8f2f23f67282dd0` at 214,209 (1,274 ms; the chain saw one nullifier and two commitments; the recipient scanned a LIVE $0.5 note at leaf 1 of the USDC.sep pool) · `unshield 1` → txid `ddf359d25d86153f4daadb6bd116ac76ada8169a9bb5af11b68a783c35593600` at 214,225 (transparent back to 14). After: USDC.sep pool 5 leaves, 2 nullifiers, conservation total 1,000,000; the legacy pool untouched. Shielding does not move `supply − burned`, so the bridge invariant held throughout.
- **The first external user of the bridge (B1.5 gate, 2026-09-09 16:42 UTC).** Eddy — the external seat operator — bridged 5 USDC from `hashkinetics.org/bridge` unprompted, within 40 minutes of the page going live: lock tx `0x313bcd3ec771f0c505bdd8962e25db471f199f259902dbe9f08a3016669825ad`, Sepolia block 11,669,182, from his own wallet, naming an account he had created through the faucet at height 210,193; Ethereum finalized it and the service minted 5 USDC.sep to `2d9fc973…` at height 211,097 (txid `af28c8e834d63c429e7b164dc4115211e54783aab50afe0325d51b9391c9f570`) — nobody at the keyboard on our side. His #testnet line, 16:50 UTC: "Just bridged 5 USDC from Sepolia to testnet-1 through the bridge page successfully." Vault 20 = supply 25 − burned 5.
- **`hk-node wallet` binds to a real account directory (`wallet_cli.rs`).** `wallet init DIR <account-dir>` takes the directory `account-new` made (sealed or plain) instead of only a devnet demo name; every submit goes through `submit_signed` — the advanced L-ratchet index is written to `account.json` before the envelope leaves the process and rolled back on a failed submit (the account CLI's reserve-then-sign discipline), and the chain's nonce wins on load. `NODE_VERSION` v0.19.1. The GUI wallets are unchanged (they predate P6 and shield the legacy asset only — an `--asset` selector is a wallet backlog item).
- **Ops.** The attestation service's config lives at `/etc/hk-attest/attest.toml` (0755): `/etc/hk` is root-only and the service runs unprivileged; secrets stay in `/etc/hk` as systemd credentials. `ops/b1-gateway.sh config` takes the files from `$HOME` or from `ops/` and refuses `0xREPLACE`; the public-sync scrub gate is blocking.

- **The first real loop (2026-09-09, Sepolia 11155111 ↔ testnet-1; `docs/BRIDGE-GUIDE.md`).** Deployed: HKVault `0x989Cb35485d16c7b19Dff890b617984EeD9970aF`, HKWrapped `wHKT.sep` `0x24159bE1577f3016AD64beA7D1430e5F2313DB26` (Circle USDC `0x1c7D4B19…`, attestor `0x1d1d8e13…` 1-of-1, cap 1,000 USDC/day, minLock 0.01; owner = the deployer, not the attestor). Assets on testnet-1: `USDC.sep` `0c3c3f40…` and `HKT` `f2b88fac…` (6 decimals, `mfps`, issuer `c58cf922…`, registered at 170,698). Service `hk-attest` on the gateway host from its own binary (the validator untouched), `confirm = finalized`, secrets as systemd credentials. **Lock 20 USDC** naming account `cf1d6719…` — tx `0x0a8d4b58165a7d3dd25c552470d6eeb0b6d8ba86b6e417b3264e5461c2930b09`, block 11,666,663, 08:02:12 UTC, depositId `0x5e8ed19f48a897fe2723964a557fc1123e00c3c52ef4f69ab9ced6f342de68b2` → Sepolia finalized it at 08:21 → **mint 20 USDC.sep** txid `2d8a80e5e7c5227fe0ad943a0d2a16ecc67205c3e9ce8127bb4b37d4ac3ccfb0` at height 188,826 (19 min, all of it Ethereum finality). **Burn 5 USDC.sep** to `7a0543ee…` — txid `9051289e40acb2b3566067d711eb3c9da2beac28aab19bb82e8fbd70baed981c` at height 190,324 (08:53) → **unlock** tx `0xfdd45642656280a4010ee929165b7a129b0e41add3a7a012c13030bcee14e046`, block 11,666,916, 08:54:36 UTC, signed by the attestor, 153,395 gas, burnId = the burn txid. After: vault 15,000,000 = `supply 20,000,000 − burned 5,000,000`; the account holds 15 USDC.sep, the Sepolia address its 5 USDC. Ops lesson kept: the config moved to `/etc/hk-attest/attest.toml` (0755) because `/etc/hk` is root-only and the service runs unprivileged — secrets stay in `/etc/hk`.
- **The reverse leg on testnet-1 (B1.4, 2026-09-09 10:20–10:48 UTC).** The issuer minted a 10 `HKT` float to the demo account (txid `25e8af86…`, height 194,176); the account burned 4 HKT naming a Sepolia address (txid `34e1a6af…`, height 194,328) → `HKWrapped.mint` 4 `wHKT.sep` tx `0x725ed38d…` block 11,667,365 about a minute later (a HashKinetics commit is final, so no wait); `HKWrapped.burn(1, account)` tx `0x0cb4ac11…` block 11,667,380 → after Ethereum finality the issuer minted 1 HKT back (txid `54d57650…`, height 195,196). After: 7 HKT on the account, `HKT` supply 11 / burned 4, `wHKT.sep` supply 3 = burned − re-minted. Both legs of both directions now have public receipts (`docs/BRIDGE-GUIDE.md`).
- **Contracts (`bridge/contracts`, Foundry).** `HKVault.sol`: `lock(amount, hkAccount)` → `Locked(depositId, sender, amount, hkAccount, nonce)`; `unlock(to, amount, burnId, sigs[])` under t-of-n EIP-712 attestor signatures (`Unlock(address to,uint256 amount,bytes32 burnId)`), each burnId once, a per-UTC-day cap, refused while paused; `HKWrapped.sol` (`wHKT.sep`, 6 decimals) for the reverse leg — `mint` under the same attestations, `burn(amount, hkAccount)` → `Burned`. Shared `AttestorSet` (strictly ascending signers, any attestor may pause, only the owner unpauses, two-step ownership, no admin path to funds) and `DailyLimit`. 26 tests (lock, 2-of-3, replay, one signature, non-attestor, duplicate, unsorted, wrong fields, wrong vault, cap + rollover, pause/unpause, rotation, validation, two-step ownership, no admin path, wrapped mint/burn/pause) + a fuzz. `script/Deploy.s.sol` from env; `README.md` with the install/test/deploy/hand-lock blocks.
- **Service (`hk-node attest-serve CONFIG.toml`; `chain/crates/hk-node/src/attest.rs`, `eth.rs`).** Sepolia watcher (eth_getLogs pages, `confirm = finalized|safe|latest|N`) → `AssetMint` of `USDC.sep` by the sealed issuer account; HashKinetics watcher (`hk_getBlock`, accepted `asset_burn` only, destination must be 20 bytes) → EIP-712 attestation → `unlock`; the reverse leg (`HKT` burn-with-destination → `HKWrapped.mint`; `Burned` → `AssetMint HKT`). Append-only fsynced JSONL ledger with the write-ahead rule (`submitting` on disk before anything leaves the process; a crash before `submitted` is HELD as needs_review, never retried by itself); `processed[id]` checked before every send; paused → `queued_paused`, cap → `queued_cap`, bad destination → `unbridgeable`; `/health` (lags, counts, held, needs_review, reconciliation `reserves == supply − burned + pending`); refuses to start on an EIP-712 domain mismatch, a wrong chain id, or an issuer that is not the asset's. `attest-cosign` (a second attestor on its own machine: recomputes the digest, checks the burn on its own node, refuses mismatches), `attest-ledger list|resolve`, `attest-key-new` (0600, prints only the address). `eth.rs` is the whole Ethereum client — keccak, ABI, RLP, EIP-1559 signing (k256), EIP-712, JSON-RPC — no alloy; 9 unit tests. `ops/hk-attest.service` (systemd credentials for the key, the passphrase, the provider URL), `ops/attest.toml.example`, `ops/B1-RUNBOOK.md`.
- **Gate `chain/gate-b1.sh`** (devnet + anvil, real proofs): lock → mint, shielded USDC.sep (shield / pay / unshield in its P6 pool), burn → unlock, restart replays nothing, `kill -9` between submit and record → held + exactly one mint + operator resolution, paused/cap queues drain, the reverse leg both ways, 2-of-2 with a cosigner that refuses a wrong amount, an unbridgeable burn, reconciliation. **Receipts 2026-09-09 (WSL):** `forge test` 28/28 (HKVaultTest 23 incl. a 256-run fuzz, HKWrappedTest 5); hk-node build clean; `eth::tests` + `attest::tests` 11/11; **`chain/gate-b1.sh` 50/50 GREEN** on a 4-validator devnet + anvil with real proofs (foundry 1.8.1): the service refused a wrong vault (EIP-712 domain mismatch); lock 20 → mint 20 USDC.sep in 8 loops; shield 2 / pay 0.5 shielded / unshield 1 in the USDC.sep pool with reconciliation intact; burn 5 → unlock 5 (`processed[burnId]` true, reserves 15,000,000 == supply − burned); a restart moved nothing; `kill -9` after the submit left the deposit in `submitting` (1 needs_review item, ok=false), the mint happened exactly once, `attest-ledger resolve` cleared it; paused → queued_paused → drained, cap → queued_cap → drained; HKT → wHKT.sep → HKT both ways; 2-of-2 with a cosigner that refused a wrong amount; a 3-byte destination → unbridgeable; final reserves 14,000,000 = 23 locked − 9 released with the unbridgeable 1,000,000 still backed.
- **The `/bridge` page (B1.5, website repo, 2026-09-09).** The Sepolia side from the reader's own wallet: live vault reserves against `USDC.sep` supply − burned (the invariant, checked in the browser), the vault's and the wrapped token's event log decoded from `eth_getLogs`, MetaMask approve + lock with the transfer followed by the page to its testnet-1 mint txid (mined → finalized → minted; survives a reload), the wHKT.sep burn-back, the CLI way back, and the trust label before anything else. No new dependency: selectors and topics from keccak (`@noble/hashes`), EIP-1193 calls by hand, two public RPCs. Gate still open: a stranger bridges 5 USDC from it.
- Docs: `docs/BACKLOG.md` (every open item in one list, swept from all plans — 8 contradictions fixed in the MBP), `docs/BRIDGE-SEPOLIA-USDC-PLAN.md` build status + the reverse-leg decision (wraps the issued `HKT`, not the genesis asset: a `Transfer` has no memo), `docs/LAWFUL-ACCESS.md` public-draft footer, the front page's shielded / lawful-access / throughput sections (website repo).

## [0.19.0] — 2026-09-09 — 🏊 P6: one shielded pool per asset (CONSENSUS CHANGE, ACTIVATION AT HEIGHT 190,000: every node ≥ v0.19.0 before it; the circuit is untouched)
- **Why now.** The bridge (B1) brings `USDC.sep` onto testnet-1 and the demo that matters is *shielded* USDC; the v1 pool is single-asset and testnet-1's is pinned to the test asset. Every issuer conversation ends at "can my asset be shielded?" (oracle plan X5). P6 answers it before the audit freeze and before the soak clock, as a consensus change rolled like X1 and G1. Design: `docs/P6-MULTI-ASSET-POOL.md`.
- **The decision: pools per asset, not assets per note.** The note commitment and the spend statement stay asset-agnostic — a note is "value units of the pool it lives in", membership is proven against one tree, so a note can never cross pools. No circuit change, no new vk pins, no wallet note-format change; the anonymity set is per asset (what an issuer expects). The shared-anonymity-set design (asset inside the note, Zcash-ZSA style) stays a later product decision.
- **State (`hk-state`).** `pools: BTreeMap<AssetId, PoolState>` next to the untouched legacy `pool`; `multi_pool_from` (config, injected by the node like `fee_from`). Routing: a **shield** goes to the legacy pool when its asset is the pinned one (or the pool is unpinned — v1 byte for byte), otherwise — from the activation height, for a registered `pool_eligible` asset — to its own pool, created on first use; a **spend** goes to the pool whose recent anchors carry its `anchor` (non-empty roots are unique across pools). Every pool seals its anchor at block end. Commitment: unchanged until a second pool exists, then a `0xB6` section (the U4/X1 trick — pre-P6 and P6 nodes agree until the first second-asset shield, which only a P6 node accepts). Conservation I5' includes the per-asset pools. `PoolKey`, `pool_key_of_asset`, `pool_key_of_anchor`, `pools_iter`, `shielded_of`. Tests: `p6_second_asset_gets_its_own_pool_and_spends_route_by_anchor` (routing, isolation of nullifiers and trees, unshield credit in the pool's asset, snapshot round trip, sealing), `p6_before_the_activation_height_a_second_asset_is_refused_exactly_as_v1`.
- **Node.** Activation table: `P6_TESTNET1_HEIGHT` (u64::MAX until the roll names it) hard-wired by chain id; other chains `HK_P6_HEIGHT` (unset ⇒ 0: devnets run multi-asset from block 1). Snapshots: `snapshot4.bin` (pools + per-asset leaf feeds, last fields) with a v3 mirror for `snapshot3.bin` (pools restore empty; a chain that had them refuses on app_hash and resyncs — never silent). Test `p6_v3_snapshot_loads_through_the_mirror`: a v0.18.x image restores at the same app_hash through the mirror, and a newer v4 image next to it wins by height. Per-pool leaf feeds routed exactly as the state routed the tx. Mempool admission checks anchor/nullifier against the resolved pool. RPC: `asset?` on `hk_getPoolInfo/Leaves/Notes/Path` and `hk_nullifierSpent` (absent = legacy — pre-P6 wallets unchanged), new `hk_getPools`, `hk_getAsset.shielded`. CLI wallet: `--asset <64-hex>` on every verb (its balance, its transfers, its own pool); `hk-node pool-path <RPC> <INDEX> [ASSET]`. `wait_tx` now returns as soon as a receipt says `rejected` (gates no longer wait 30 s per refusal).
- **Gate:** `chain/gate-p6.sh` — **48/48 GREEN on 2026-09-09** (first run 44/4: the four were gate-side — a racy live-tip app_hash compare, now compared at a fixed height, and `dollars()` printing whole units; the chain routed every case correctly the first time) (register USDC.t `mfps` + EUR.t `mfp`; legacy pin; second pool with its own ledger; EUR.t refused; per-pool scan; unshield credits USDC.t and the nullifier lives in its pool; shielded pay to a second wallet; pool-path with the asset; restart from snapshot4 with both pools; sync from genesis derives both; a `HK_P6_HEIGHT=999999` devnet refuses with the v1 `pool asset mismatch`).
- **Regression on the shipped binary `3338ec2c…`:** gate-x1 40/40 · gate-h3 22/22 · gate-n1 20/20 · rehearsal GREEN (hk-state + hk-node unit suites inside it).
- **Release + roll.** `P6_TESTNET1_HEIGHT = 190,000`, named 2026-09-08 21:53 UTC at tip 163,659 with the chain at 1.80 s/block (WUMP still on v0.18.1): ≈ 13 h at that rate, ≈ 7 h at the 1 s floor — the founder chose ~12 h of notice (the G1 pattern: deadline notice, founders first, externals told again ~1 h before; old binaries island at the first second-asset shield after the height). Release: `hk-node-v0.19.0.gz` sha256 1b290c8f754a2046129cd3d192c65a33b665b1e12ebe7de6b27f6559d344ba33 (binary 3338ec2c9e93f46816cfa9dea2eccbdf2f96f92109407f32391456d6d17b4820); text `ops/RELEASE-BODY-v0.19.0.md`; roll `ops/roll-v0.19.0.sh` — released 2026-09-08 22:28 UTC (immutable, Latest, one asset `hk-node-v0.19.0.gz`, GitHub-computed sha256 `1b290c8f…` == CHECKSUMS); #testnet deadline notice 22:34 UTC with the hour from `eta` (height 164,932 · 1.71 s/block · 24,998 blocks · ≈ 10:27 UTC 09-09, ≈ 05:29 UTC if every seat runs at the 1 s floor); **fleet rolled 22:39–22:48 UTC** (val-1 → val-0 → val-2 → gateway, each proven VOTING before the next: signer.remaining 30239→30219 · 6144→6123 · 12406→12390 · 28317→28270; RPC back 10–17 s after each restart; the chain never paused); `pools` on all four seats: count 1 · multi_pool_from 190000 · legacy_asset 09090909; `status`: hk-node v0.19.0 ×4 at heights 165,564–165,601; public RPC `hk_getPools`: count 1 · multi_pool_from 190000 (legacy pool 20 leaves · 8 nullifiers · 58,990 shielded); external seats at the roll: two on v0.18.2, one still on v0.18.1. Desktop/Android wallets stay on the test asset until P6.2 (asset picker + per-asset scan cursor in `hk-wallet-core`); explorer pool-per-asset view P6.2.

## [0.18.2] — 2026-09-08 — ⚡ R15: fast consensus signing — 179 µs per signature instead of ~450 ms (client-only; no rule, no deadline; wallet stays v0.14.1, Android 0.2.0)
- **Found by an external operator.** On 2026-09-08 Eddy (seat 175D9A8E…, on v0.18.1 since the first external admission) reproduced on the v0.18.1 sources what his seat had been showing since it joined: every consensus signature cost **447.7 ms median, 787 ms p95** of pure CPU (H10/H5 consensus key; an H5-only signer 12.3 ms) — and posted the A/B, not a complaint. Our first diagnosis (storage latency on the per-vote fsync) was wrong; the persistence bench is a few ms. Corrected in the open the same day.
- **Two causes, confirmed in code while building the fix.** (1) `hbs-lms` `hss_expand_aux_data` compared its 32-byte HMAC against *everything* after the cached layers; keygen shrinks only the slice it is handed (`get_expanded_aux_data`), never our 256 KiB `aux` Vec, so at sign time the constant-time compare ran against ~256 KiB and failed on every call → the cache was silently unused and the H10 top tree's authentication path was regenerated from ~1,000 LM-OTS key generations per signature (that is the ~450 ms). (2) `hss_sign` re-parses the compressed key every call: `HssPrivateKey::from` regenerates the H5 child tree and re-signs its public key with the H10 parent, per signature.
- **The fix — `vendor/external/hbs-lms-rust/src/hss/{aux.rs,mod.rs,lib.rs}` (patched vendored crate, copied to the public tree by `sync-public-freshness.sh` 1c) + `chain/crates/hk-crypto/src/hashsig.rs`.** The MAC compare is bounded to `H::OUTPUT_SIZE` (still HMAC-checked, seed-keyed; a corrupt cache is still rejected → recompute). New `HssSigningSession` keeps the expanded key and a full node cache of the current bottom tree between signatures (`open / sign / pending_private_key / commit / rollback`), rebuilds from the committed compressed key at rollover or after a rollback, and advances the compressed key by the crate's own odometer. `HashSigner.sign` = session.sign → persist `used+1 ‖ state` atomically → commit; a failed write rolls the session back (nothing released, the same leaf signs next); the session is dropped whenever the state is replaced from outside (`load_state`, `attach_persistence`). **Signatures byte-identical** (deterministic LM-OTS), on-disk state format unchanged, verification untouched — v0.18.1 and v0.18.2 seats interoperate; no consensus rule, no activation height.
- **R15b — the block-time floor (same release).** With signing at ~0.2 ms the 4-seat devnet decided every **0.11 s** (8.9 blocks/s, from 1.48 s) — and a chain that runs as fast as its round trips grows its block log by kilobytes of hash-based signatures per block with no upper bound, a restarting seat must out-run the tip to rejoin, and a slow seat drops out of every certificate. So the proposer paces itself: at round 0 it waits out the remainder of `HK_MIN_BLOCK_INTERVAL_MS` (default **1000 ms**; 0 = unpaced) since its own last commit before building; rounds > 0 never wait; the wait is clamped to half the propose timeout so a bad value cannot cause round changes. Local behaviour, not a rule: paced and unpaced nodes agree on every block (`hk-node/src/app.rs` `GetValue`, `state.rs` `propose_wait` / `note_decided`). `bench-seats.sh` runs unpaced (it measures the raw cost per seat) and prints the floor in its row. Expected on testnet-1 after the founders roll: ~1 s blocks (from 3.2 s) with the fleet's disk growth ≈ 3× today's, not 30×.
- **Measured (release build, WSL, 2026-09-08):** `r15_cached_signing_is_faster` → **`R15 median per signature: cached 179 us, uncached 18456 us`** (uncached = the old path with only the cache fix; as shipped ≈ 450 ms). Tests: `r15_cached_signing_matches_uncached_across_rollover` (70 signatures, two H5 rollovers, byte-equal signatures and states), `r15_persistence_failure_retries_the_same_leaf`, `r15_restart_resumes_identically`; vendored crate: `signing_session_matches_hss_sign_across_rollover`, `signing_session_rollback_reuses_the_unreleased_leaf`, `aux_cache_validates_from_an_oversized_buffer`. hashsig suite 10/10. Devnet: **gate-h3 22/22 · gate-n1 20/20 · rehearsal GREEN** on the shipped binary `93842255…` (hk-node + hk-state unit suites 21 + 33 green inside it). Two honest notes: gate-n1 first ran **16/4 RED on the unpaced binary at 9 blk/s** (node-4 join/sync at that rate) and passed 20/20 once the R15b floor was in; and two RED rehearsal runs before the green one launched no node at all — a relative `CARGO_TARGET_DIR` in the runbook command, not the code (fixed to `$PWD/target` in the checklist). **4-seat bench after: raw (floor 0) 8.773 blk/s · 0.11 s · 55 MiB/node; paced (floor 1000 ms) 0.908 blk/s · 1.10 s · 32 MiB/node** — before, on v0.18.1: 0.675 blk/s · 1.48 s · 32 MiB.
- **Security, plainly:** no new secret in memory (the expanded key is derived from the seed the node already holds; `LmsPrivateKey` is `ZeroizeOnDrop`); the one-time-leaf guard is unchanged (reserve-then-sign, rollback, restart from disk) and tested; the patch lands inside the audit's frozen tag (`docs/AUDIT-SCOPE.md`).
- **Consequences.** The seat budget in `docs/CAPACITY-SHEET.md` §f (N = 32) was measured with the slow signer and is now conservative — re-bench 8/16/32/64 when the box is free. `docs/VALIDATOR-ONBOARDING.md` §0 no longer blames the disk: a slow CPU is what kept a seat out of round 0, and v0.18.2 removes most of that cost; the NVMe advice stays (the fsync per vote is real, just small). `timeout_propose` stays 2 s until the WAN seats report their round-0 rate on v0.18.2. Release: `hk-node-v0.18.2.gz` sha256 581ef97e0e2017d16bbc476a38e6906d1b5ab439a2836d7a33ed47865a439235 (binary 93842255daae83dd916c3c4e5d004259c9e8917bed5c21a40361b6e643c6c4bc); text `ops/RELEASE-BODY-v0.18.2.md`; roll `ops/roll-v0.18.2.sh` — **Roll (2026-09-08 17:57–18:07 UTC, `ops/roll-v0.18.2.sh` from Cloud Shell; release published 17:37 UTC, asset digest == CHECKSUMS):** binary `93842255…` fetched from the release, sha-verified on all four hosts before any start; K5/K6 precheck green (kit `vks.json` b7776b06 in every home, unit + public prover ok); val-0 and val-2 reported `NeedDaemonReload=yes` (on-disk unit == loaded unit — reloaded before their roll); rolled val-1 (17:57:51, RPC up 4 s later, VOTING 26,301 → 26,288 @ 156,667 → 156,682) → val-0 (18:01:09, RPC +18 s, 27,516 → 27,499 @ 156,709 → 156,726) → val-2 (18:04:03, RPC +16 s, 8,144 → 8,135 @ 156,753 → 156,773) → gateway (18:06:54, RPC +18 s, 24,908 → 24,870 @ 156,813 → 156,827; faucet back, 50,505 drips, cooldowns restored; bot restarted), one voter at a time, each proven VOTING before the next; the chain never paused; resident 55–60 MiB per seat (was 111–119). Power view identical on all four after: `seats 7 · total 19 · founding 16 · external 3 · quorum 13 · founders_alone True · bootstrap {110000, 4, active True}`. **Cadence receipt (public RPC, 120-s windows): before 3.53 s/block (34 blocks, height 156,501 @ 17:46:49Z) → after 2.95 s/block (41 blocks @ 18:10:21Z) with the three external seats still on v0.18.1.** Per-height wall clock at ~18:20 UTC (polling the tip): **every founding seat's height 1.0 s — the floor, exactly**; Eddy's seat **1.0 s** (he was on v0.18.2 within 30 minutes of the notice — `hk_getPeers` shows his node `v0.18.2 match`); JK's (v0.18.1) 2.0 s; WUMP's (v0.18.1) **7.0 s** every cycle (round 0 times out at 2 s, round 1 too, a founder commits in round 2) → the chain runs ≈ 2.0 s/block until the last two seats upgrade, then 1.0 s. Commit certificates carry the four founding signatures at this pace (the v0.18.1 seats' precommits arrive after the commit) — the external votes return to the certificates as each seat upgrades.; announcement `ops/DISCORD-v0.18.2-2026-09-08.md`.

## [wallet-android 0.2.0] — 2026-09-08 — 📱 the Android wallet: first APK out of CI (0.1.0, WA1–WA3 closed), then the brand theme (0.2.0; node stays v0.18.1)
- **WA1 — `chain/crates/hk-wallet-core` (the library behind the app).** The desktop wallet's account / vault / shielded code behind a UniFFI 0.29 surface (33 `Wallet` methods, records `Endpoints` / `WalletState` / `Status` / `TxResult` / `NoteView` / `ScanResult`, a `Progress` callback for the activity panel, `WalletError::Message`); `mobile_profile()` = Argon2id 256 MiB / t3 / p4 — the parameters ride in the HKE1 envelope, so a desktop opens the file. **Gate `chain/gate-wa1.sh` 19/19 GREEN:** unit tests; the whole journey on a fresh devnet + faucet + prover (create → faucet → send → shield → scan → unshield → pay shielded with a memo → disclose → protect → lock → unlock → send from the sealed files); byte-compatibility — the CLI opens the core's sealed `account.json` with the passphrase, refuses without it, `account-send` from the core's directory is accepted on-chain, `verify-disclosure` accepts the core's package; the cdylib carries the UniFFI metadata.
- **WA1b — `chain/android-core.sh`:** `cargo ndk` (NDK r27) → `libhk_wallet_core.so` for arm64-v8a + x86_64, Kotlin bindings generated from the host cdylib's own metadata (`uniffi-bindgen --library`, package `org.hashkinetics.wallet.core` from `uniffi.toml`).
- **WA2 — `android/` (Kotlin + Jetpack Compose, minSdk 26, AGP 8.6.1 / Kotlin 2.0.20 / compose BOM 2024.09).** `WalletVm` serialises every core call on one mutex off the main thread; `DeviceKeyfile` = the optional key-file second factor, 32 bytes wrapped by an AES-GCM key in the Android Keystore (off by default so a phone backup restores on a PC with the passphrase alone). Files on the phone are the desktop's (`account.json` / `shield.json` / `disclosure-*.json`), `allowBackup=false`.
- **WA3 — `.github/workflows/wallet-android.yml`:** cross-compile + bindings + `assembleDebug` on every push that touches the wallet; the APK and its sha256 as a run artifact. **First green run #4 (2026-09-08, 6 m 32 s): `HashKinetics-Wallet-android-0.1.0-debug.apk` sha256 `5525a79d…`**, sideloaded and working on a phone. Runs #1–#3 taught: invoke scripts with `bash` (NTFS checkouts lose the exec bit), sync `uniffi.toml` or the bindings land in the wrong package, provision Gradle 8.9 (AGP 8.6 cannot configure under the image's Gradle 9), and never name a function `setX` next to a property `x` (JVM setter clash).
- **0.2.0 — the theme.** The engineering-dark design system of the site and the desktop wallet (`ui/Theme.kt`: #06080F ground, navy panels, cyan actions, violet for the shielded side, gold for warnings, monospace for bytes), the brand mark as an adaptive launcher icon (gradient hexagon + HK + kinetic slashes, drawn as vector paths; themed-icon layer for Android 13+), dark splash, edge-to-edge. Four sections behind a bottom bar — Wallet (balance card, **receive with a QR** of the account id, send) · Shielded (hidden balance, stealth address **with QR**, notes, shield / unshield / pay / disclose) · Backup (seed, passphrase, device lock, key-file export) · Activity — plus Welcome and Unlock screens; copy buttons for ids and addresses; the chain is read on open. Same core, same files.
- **Released — 2026-09-08 09:16 UTC, `wallet-android-v0.2.0` (immutable, pre-release): `HashKinetics-Wallet-android-0.2.0.apk` sha256 `3510d1c8bad8506ff406ea64ae6a7a7a06a3b68e2a6ec4539b0cdda6919d3904` (GitHub's asset digest agrees), signer certificate SHA-256 `b296799ed6bea902f6a30ed3bcd497adce7374eb15ec9f8d587ef0575902d6d3` (valid to 2054-01-24), built by run #8 on the tag (3 m 48 s; the tag = versionName check passed); row in `networks/testnet-1/CHECKSUMS`. First release under the persistent key — every later build upgrades in place.**
- **Signing (WA3).** The release key is a repository secret, never in the tree: CI decodes it into a temp file for the one Gradle step (`HK_ANDROID_KEYSTORE*` env → `signingConfigs.release`; forks and machines without it fall back to the debug key and the artifact is named `-debug`). Every run ships the APK with its sha256 and the signer's certificate digest (`apksigner verify --print-certs`); releases are tag-first (`wallet-android-vX.Y.Z` must equal `versionName` — the workflow refuses otherwise), hand-made and immutable, with a `CHECKSUMS` row. Sideload only until the Play listing. Unaudited testnet software; nothing here is for sale.

## [0.18.1] — 2026-09-06 — ⚖️ G1 activation moved to height 110,000 (v0.18.0 withdrawn, never rolled) — every node ≥ v0.18.1 before 110,000
- **One constant.** `G1_TESTNET1_HEIGHT` 200,000 → **110,000** (`hk-node/src/genesis.rs`; unit test updated). Yadu, 14:05 UTC, reading the ETA (~91 h to 200,000): the exposure G1 removes — a set change needing an external co-signature, liveness resting on two external VPSs — was live that day, and four days was too long to leave it; the founding fleet rolls within the hour, the external operators are told the height and the hour, and a node that misses it islands at 110,000 and rejoins when it upgrades (replay by rule). Nothing else changed: same `SetPower`, same RPC view, same gate.
- **v0.18.0 is withdrawn** (immutable release, text marked; never rolled to any founding seat): it names 200,000, so a node running it counts power 1 per seat at 110,000 and islands there exactly like a v0.17 node. The rule of the activation table holds — the number moved by patch release BEFORE it was reached; it never moves after.
- **Receipts:** build + `cargo test -p hk-node -- g1_` on the new constant, gate-g1 (env-driven devnet height, unaffected) re-run **40/40 GREEN**, rehearsal **GREEN** on the shipped binary, `g1_activation_is_hardwired…` on the new constant, build 3 m 22 s; shipped binary `469cf4833e9d72da937b517791495443a214d40d46e7d5a35f2808f35dcfaaaa`, gz `70b8c7cee372a3216b07b06099e2e7c574262c4e8ad769084a2b2119dcf861d7`. Roll + activation: see the roll bullet below.
- **Roll (2026-09-06 14:32–14:38 UTC, `ops/roll-v0.18.1.sh` from Cloud Shell; release published 14:28 UTC, asset digest == CHECKSUMS):** binary `469cf483…` verified on all four hosts before any start; K5/K6 precheck green (kit `vks.json` in every home, prover reachable); deadline guard 4,250 blocks; rolled val-1 → val-0 → val-2 → gateway (+faucet, +bot), one voter at a time, **each restarting from its SEALED key and proven VOTING before the next** (val-1 20897 → 20875 @ 105766 → 105775, swap → RPC 6 s · val-0 10558 → 10537 @ 105798 → 105808 · val-2 25791 → 25771 @ 105831 → 105840 · gateway 20489 → 20472 @ 105860 → 105866, faucet back with 50,506 drips); every first start clean (the fleet boots from the kit's `vks.json` since the morning — no K5 refusal this time); the chain never paused (105,750 → 105,866 during the roll), quorum 5 of 6 held with both external seats up. Power view identical on all four right after: `seats 6 · total 6 · founding 4 · external 2 · quorum 5 · max_absent 1 · founders_alone false · bootstrap {110000, 4, active false}`; the public RPC answers `node_version v0.18.1` and the same view with `genesis: false` on the two external seats; peers 6 (Eddy v0.16.0, heheda_123 v0.17.0, WUMP v0.16.0 — all three reconnected within two minutes). #testnet deadline notice posted 14:50 UTC (ETA 18:31 UTC at 3.36 s/block). **ACTIVATED — block 110,000 committed 18:43:23 UTC (2026-09-06; public RPC: 109,999 seen 18:43:21, 110,000 seen 18:43:23; the view flipped at tip 110,000); from 110,001 the public RPC answers total_power 18 · founding_power 16 · external_power 2 · quorum_power 13 · max_absent_power 5 · founders_alone_decide true · bootstrap.active true, powers 4-4-4-4-1-1 (42C00CD6 · E77F5F47 · F7D75524 · F874765D at 4 with `genesis true`, 175D9A8E · 97E16693 at 1; count still 6, no pending set change); app_hash after 110,000 `f0e1f54c94e5500bdf9cb3e39cf7868c972e1f4a2accd27e8bed6274d4b7c537` = block 110,001's `parent_app_hash`, no `set_changes` in block 110,000 (a rule, not a certificate), every commit certificate from 110,001 on in round 0 with 4–5 of 6 seats signing (quorum 13 of 18 needs at least three founding seats on every block), the gateway seat's `signer.remaining` 10,803 → 10,651 across 110,029 → 110,097 (voting); cadence unchanged (97 blocks in the 5 min after: 110,000 → 110,097 by 18:48:38 UTC, 3.24 s/block vs 3.38 over the 20 min before); external nodes: all three followed straight through — Eddy 37.60.237.0/24, 679 WUMP 43.155.163.0/24 and heheda_123 101.33.68.0/24 on v0.18.1 with `genesis match`, connected 10,518–13,059 s without a reconnect, `islands_refused 0` — nobody islanded. Per-seat journal lines (`G1 BOOTSTRAP GOVERNANCE ACTIVATED` on the four founding seats) and the four-way `app_hash` compare: every founding seat logged `G1 BOOTSTRAP GOVERNANCE ACTIVATED — genesis seats re-weighted` at **18:43:28.18 UTC** within a millisecond of each other (val-0 .180545 · val-1 .179958 · val-2 .179796 · gateway .179615; `height=110000 founding_power=4 founding_total=16 total_power=18 seats=6 quorum=13 effective_from=110001 replay=false` on each — read back 2026-09-07 13:25 UTC from the journals, `~/g1-journal.log`); `status` the next morning: all four on v0.18.1 with no restart since the roll (uptime ≈ 82,300 s), epochs 10/11/11/11 (the founders rotated under the new weights unattended overnight), resident ≈ 90 MB each, all voting on the same certificates.**
- **Testnet-1 receipt — the sixth external voting seat, #10 (2026-09-11 ≈ 11:59 UTC): the first professional operator.** 1XP (JinWoo Lee, Seoul; PoS infrastructure since 2018, 30+ networks, Wormhole Guardian operations, ISO 27001; validator.json by e-mail 2026-09-11 11:23 UTC). Gate: node on the roll call as `genesis match`, `v0.19.0`, `5.9.8.0/24`; his own `hk_chainInfo` at height 318,638 — app_hash `ca404d05…`, genesis `4e4ea68d…` — verified independently against `hk_getBlock(318639).parent_app_hash` on the public RPC (he had checked it himself too); at the tip when reported; named operator. Certificate `Admit` root `f350f55e…` power 1, approved by the **four founding seats' SLH-DSA-192s roots** (3fc0cf82…, 0dc77ed1…, eff24e00…, b2709fad…), 16 of 21 > ⅔, window 319,342–359,292, submitted on the gateway (`accepted:true, approvals:4, queued:true`), **committed in block 319,494, effective 319,495: validator set 9 → 10, power 22, quorum 15, `founders_alone_decide` still true (16 > 14⅔)** — `F09B82019257EA933A7B8BDD2A6872531E97B10E` joins with `genesis: false`. External power 6 of 22. No new genesis, chain never paused; the whole admission — check, propose, four approvals, submit, commit — took eleven minutes from the operator's e-mail. A seventh external candidate (validator.json on file, node on the roll call on v0.19.1) still waits on its height + app_hash. From the twelfth seat on, admissions need an external approval or the bootstrap handover: 3×16 = 48 is no longer > 2×24.
- **Testnet-1 receipt — the fifth external voting seat, #9 (2026-09-11 ≈ 05:07 UTC).** Operator qianxun (moniker `vps1`; validator.json via Discord 2026-09-10; node synced from genesis on `v0.19.0`, on the roll call as `genesis match` for ~14 h; his own `hk_chainInfo` at height 301,740 — app_hash `2cdf3f59…`, genesis digest `4e4ea68d…` — matched `hk_getBlock(301741).parent_app_hash` on the public chain, at the tip when reported — the gate: version ≥ the activation release, genesis match, hash match at a stated height, at the tip, named operator). Certificate `Admit` root `68ca3352…` power 1, approved by the **four founding seats' SLH-DSA-192s roots** (3fc0cf82…, 0dc77ed1…, eff24e00…, b2709fad…; sealed keys, passphrase piped on each host), 16 of 20 > ⅔, window 303,108–343,058, submitted on the gateway (`accepted:true, approvals:4, queued:true`), **committed in block 303,291, effective 303,292: validator set 8 → 9, power 21, quorum 15, `founders_alone_decide` still true (16 > 14)** — `79812BC7432CF5FC2C58C6D6C5BD91FEDF6222C1` joins with `genesis: false`. External power 5 of 21. No new genesis, chain never paused; seat count went 8 → 9 on all four founding hosts at the same height (`ops/admit-seat.sh` check → propose → approve ×4 → `NEED=4 submit` → watch, log `~/admit-vps1.log`). A sixth external candidate (validator.json + multiaddr by e-mail 2026-09-11, node on the roll call as `v0.19.1`, `genesis match`, ~2.6 h) waits on its height + app_hash.
- **Testnet-1 receipt — the fourth external voting seat, #8 (2026-09-09 ≈ 16:10 UTC).** Operator Oleg Shandra (moniker `hk-test`; validator.json by e-mail 2026-09-08 19:41 UTC; node synced from genesis, `v0.19.0`, on the roll call as `genesis match`; his own receipt at height 6,620 matched `hk_getBlock(6621).parent_app_hash`; at the tip (205,359) before we submitted — the gate: version ≥ the activation release, hash match, at the tip). Certificate `Admit` root `da3c3efa…` power 1, approved by the **four founding seats' SLH-DSA-192s roots** (3fc0cf82…, 0dc77ed1…, eff24e00…, b2709fad…; sealed keys, passphrase piped on each host), 16 of 19 > ⅔, window 185,238–225,188 (proposed 2026-09-09 ~07:00 UTC, held ~9 h until his node reached the tip), submitted on the gateway (`accepted:true, approvals:4, queued:true`), **committed in block 209,149, effective 209,150: validator set 7 → 8, power 20, quorum 14, `founders_alone_decide` still true (16 > 13⅓)** — `06EF4D8749BD61F3617E48CDDF3666FEF8CAD4D5` joins with `genesis: false`, app_hash at 209,149 `e6e62fed…`. External power 4 of 20. No new genesis, chain never paused; certificates 4 signatures before and after the height. `ops/admit-seat.sh` check → propose → approve ×4 → `NEED=4 submit` (run twice by mistake — the same certificate; the second was a no-op), logs `~/admit-oleg.log`, `~/submit-seat8.log`. The soak clock still waits on the bootstrap handover (`SetPower`), not on seat count.
- **Testnet-1 receipt — the third external voting seat (2026-09-07 ≈13:35 UTC; the first admission on founding approvals alone under G1).** Operator heheda_123 (JK; node on v0.18.1 on the roll call for 22 h, `genesis match`) — certificate `Admit` root `6badbf7f…` power 1, approved by the **four founding seats' SLH-DSA-192s roots** (3fc0cf82…, 0dc77ed1…, eff24e00…, b2709fad… — every signature from a SEALED key, passphrase piped on the host), 16 of 18 > ⅔, window 128590–133540, submitted on the gateway (`accepted:true, approvals:4, queued:true`), **committed in block 128,613, effective 128,614: validator set 6 → 7, power 19, quorum 13, max absent 6, `founders_alone_decide` still true** — `B6A797E36D7F9C809702D16CD574277702B108D9` joins with `genesis: false`. The morning's stale proposal (window 101894–141844) and its approvals were retired unused. No new genesis, chain never paused (the seat count went 6 → 7 on all four founding hosts at the same height; `ops/admit-seat.sh` check → propose → approve ×4 → `NEED=4 submit` → watch, logs `~/admit-jk-*.log`). External power 3 of 19. **Signing receipt (read from the public RPC 2026-09-08 ~10:00 UTC):** the seat's proposer slot (index 6 of 7, heights with (h−1) mod 7 = 6) timed out in round 1 at every turn from admission until height **130,277**, and has proposed in round 0 at every turn from **130,284** onward — ~18,000 blocks, ~2,570 slots, zero timeouts; peer connected ~18 h, genesis match. The seat votes.

## [0.18.0] — 2026-09-06 — ⚖️ G1: bootstrap governance — the genesis seats weigh 4 from height 200,000, `SetPower` by certificate, the quorum line readable (CONSENSUS RULE, ACTIVATION BY HEIGHT: every node ≥ v0.18.0 before testnet-1 height 200,000; wallet stays v0.14.1)
- **What forced it (the 2026-09-06 decision).** Two external seats had joined at power 1 (0.15.2 receipts): six seats, six power, quorum 5. Two consequences nobody had signed up for: a set change now needed an **external co-signature** (4 of 6 is not > ⅔), so the seventh operator's admission — prepared and signed by all four founding seats the same morning — would have hung on a seat that was three days old; and the chain's **liveness depended on two external VPSs** staying up (both down = halt). The founder's call: no co-sign from a seat that young, and no reliance on external machines to keep the chain moving while the network is this small — fix it at the protocol level, published, dated, undone by the same mechanism. The signed proposal (window 101894–141844) was left to expire unused; the operator was told he is seated the moment v0.18 is active.
- **The rule (`hk-consensus::reweight_roots`, `hk-node/src/genesis.rs::bootstrap_for`, `state.rs::apply_set_changes`).** At the **activation height** every node re-weights the **genesis seats** (the roots in `genesis.json` `validators[]` — the four ceremony keys) to the **founding power**, in place: operational key, epoch and address untouched, effective `height + 1`, pushed into the HK-R6 set history like any certificate. No proposal, no signatures — the rule is in the binary every node runs, exactly as authoritative as the genesis itself, applied by the same code on a live commit, on restore-from-store replay and on a sync from genesis (a snapshot taken after the height already carries the weights). A genesis seat removed before the height is not re-seated; a seat admitted by certificate is never touched. **testnet-1 (`hashkinetics-1-4e4ea68d`): height 200,000, power 4 — hard-wired by chain id; the environment cannot move it.** Other chain ids (devnets): `HK_G1_HEIGHT` / `HK_G1_POWER` (default 4), unset = no activation. Unit: `g1_reweight_roots_touches_only_the_named_seats_and_is_idempotent`, `g1_activation_is_hardwired_for_testnet1_and_env_only_elsewhere`.
- **The arithmetic (testnet-1; quorum = `2·total/3 + 1`).** Founding power 16. With the two external seats of today: total 18, quorum 13 — the four founders decide alone (16), or three founders with one external (13); founders pass a set change alone (3·16 = 48 > 36). Weight 4 holds strictly more than ⅔ up to **seven** external seats of power 1 (48 > 2·23 = 46; the eighth breaks it: 48 = 48). What the founders give up: one founding seat absent **and** every external seat absent still halts the chain (12 of 18) — the founding fleet keeps all four seats up, as it always has. What nobody can do any more: stall the chain, or block a change, by switching off external machines. The founder's own note on the table: 16 ≯ ⅔ × 24, so the ceiling is seven, not eight.
- **`SetChange::SetPower { root_pk, voting_power }` — the handover tool (signing tag `0x03`; power ≥ 1, use `Remove` to unseat; idempotent when the power already matches; the set's ordering is by power then address, so the proposer schedule shifts at the height exactly as an admission shifts it).** Same approval rule as `Admit`/`Remove` (> ⅔ of the current power, window, chain id). `hk-node set-change propose <HOME> --set-power <root_pk hex> --power N …` then approve / assemble / submit as before. The founders' weight comes back down by this certificate on a dated milestone (`docs/MASTER-BUILD-PLAN.md` §7.1: handover → then the G3 soak clock) — one seat per change, a receipt per change, never a binary. Unit: `set_power_certificate_changes_one_seat_in_place` (a 0-power body refused by shape; the signing bytes distinguish `SetPower` from `Admit`/`Remove` on the same root).
- **Read it back — `hk_getValidators` (`rpc.rs`):** `founding_power`, `external_power`, `quorum_power`, `max_absent_power` (how much power may be down before the chain stalls), `founders_alone_decide`, `bootstrap {height, founding_power, active}`, and `genesis: true|false` per seat; `pending_set_changes[].change` / `hk_getBlock.set_changes[].change` read `set_power`. Log lines: `G1 BOOTSTRAP GOVERNANCE ACTIVATED — genesis seats re-weighted` (every node, same height) and `Validator RE-WEIGHTED (G1 set-power)`.
- **Activation discipline (the third one, after `AccountCreate` v0.11.0 and V1 v0.14.0).** Every node must run **≥ v0.18.0 before height 200,000**: a ≤ v0.17 node keeps counting power 1 per seat, refuses the first commit certificate after the height it cannot make a quorum of (founding votes only: 16 of 18 is valid, 4 of 6 is not) and stops following, loudly; it also cannot decode a block carrying a `SetPower` certificate. The founding fleet rolls first (val-1 → val-0 → val-2 → gateway, one voter at a time, each proven voting, `ops/roll-v0.18.0.sh`); the external operators are told the height and the day in #testnet ≥ 3 days ahead; the `/network` roll call shows which versions are seen. The `hk-status` Discord bot's `SEATS ABSENT` / `QUORUM AT RISK` alerts (v2.2, 2026-09-06) are the stopgap for the power-1 rule until the height.
- **Gate — `chain/gate-g1.sh` (activation at devnet height 40 from the environment; receipt: `gate-g1.sh` **40/40 GREEN** (first run 38/40 — both misses in the harness, not the node: the set-power log hunt did not strip tracing's ANSI colouring, and the post-restart app_hash compare read two tips a height apart; fixed to compare at the same height, re-run 40/40); `gate-v1.sh` 25/25; `gate-n1.sh` 20/20; rehearsal GREEN on the shipped binary (one timing flake on the first run, clean on the re-run); unit: hk-consensus `setchange` 5/5, hk-node `g1_`/`r11_`/`keys`/`h3` 8/8; build 3 m 43 s; shipped binary sha256 `bdaefd4a…`, gz `c25ca0aa…`):** before the height 4 seats · power 1 · quorum 3 · `bootstrap.active false` · every seat `genesis`; at H+1 on every node power 4-4-4-4, total 16, quorum 11, `founders_alone_decide`, the activation log line, identical `app_hash` across the four, the chain keeps deciding; a fifth (external) seat admitted with **three** founding approvals (12 > ⅔ of 16) → total 17, `external_power` 1, not `genesis`; `SetPower` 1 → 3 by certificate → total 19, quorum 13, `set_power` in `hk_getBlock`, power 0 refused by shape; the external node killed → the chain advances on the founders (16 > ⅔ of 19); node0 restarted on its persisted home re-derives the weights and the app_hash; node5 from genesis syncs across the activation and both certificates. Regression: `gate-v1.sh`, `gate-n1.sh`, rehearsal on the shipped binary.
- **Docs:** `V1-VALIDATOR-SET-CHANGES.md` §6 (the rule, the table, the handover, the discipline), `VALIDATOR-ONBOARDING.md` (header, §7 "what your seat weighs", §8a), `RPC.md`, `README` honesty ledger, `MASTER-BUILD-PLAN.md` / `C-PROGRAM-PLAN.md` / `P3.2-IMPLEMENTATION-PLAN.md` (the handover milestone sits before the soak clock).
- **Roll:** none — WITHDRAWN the same evening by [0.18.1] (activation moved to 110,000); no founding seat ever ran v0.18.0.
- Unchanged: state machine, `app_hash` (the validator set was never part of it), storage, keys, wallet (v0.14.1). No new dependencies. `hk-node --version` → v0.18.0.

## [0.17.0] — 2026-09-06 — 🪶 R11: the node verifies with a verify-only SP1 client — seconds to start, a resident set a stranger's box can carry (client-only; wallet stays v0.14.1)
- **The one line (`hk-node/src/verifier.rs`).** `ProverClient::builder().cpu().build()` → `ProverClient::builder().light().build()`. Since the first shielded block the node had built SP1's FULL CPU prover to call `verify`: the recursion programs for every shard shape, the shrink and wrap provers — the whole proving engine, at every start. On the fleet's 2-vCPU hosts that was **3–6 minutes** from `systemctl start` to an answering RPC and a **~6.7 GB** resident set that never produced a proof (INCIDENTS #6; the "8 GB minimum, 16 GB comfortable" line). sp1-sdk 6.4.0's `LightProver` builds the verifier machines and carries the pinned recursion-vk root; `prove` on it is an error by design. Verification is the SAME code path (`SP1Verifier` through `SP1NodeCore`) with the same embedded vk root — the SDK asserts the light and full clients agree in its own tests — so the accept set and the refuse set are unchanged: client-only, no consensus impact. The node never had a reason to prove; now it cannot. The binary shrank with it — **70.1 MB → 30.6 MB unpacked (24.6 MB → 9.8 MB gzipped)**: the proving engine, its recursion artifacts (`vk_map`) and the shrink/wrap provers are no longer linked at all, because nothing in the node references them (checked with `strings`: `SP1ProverEngine`, `vk_map`, `wrap_prover`, "initializing cpu prover" are absent from v0.17.0; the light-client line is present).
- **`hk_chainInfo.process {rss_bytes, uptime_secs, verifier_init_ms}` (`rpc.rs`, `state.rs`).** Resident set from `/proc/self/statm` (Linux; null elsewhere), seconds since `start`, and how long the client took to come up — the RAM line in the onboarding doc becomes a number any operator checks with one call. The startup log line `verify-only SP1 client ready` carries `init_ms`, the SP1 circuit version and `rss_mib`. Unit: `r11_rss_is_read_from_statm_pages`, `r11_verifier_init_is_recorded_once`.
- **Measured (WSL devnet, gate-r11, 4 nodes on one box):** verifier init **379 ms** (219 ms on a restart) · **52 MiB** resident right after start, **259–309 MiB** per node after mint + spends + a compressed aggregate · first log line → RPC listening **1 s** · node0 killed and restarted on its persisted home: RPC back in **2 s**, in step with node1 within seconds. **Fleet (e2-standard-2, sealed keys, rolled 2026-09-06 09:54–10:03 UTC):** ~6.1–6.7 GB → **54–59 MiB** resident per seat, verifier init **0.8–1.4 s**, start → RPC **6–16 s** (was 3–6 min) — the per-seat receipt is the roll bullet below.
- **Gate (receipt: `gate-r11.sh` 29/29 GREEN, `gate-n1.sh` 20/20, rehearsal GREEN on the shipped binary):** `chain/gate-r11.sh` (needs hk-prove) — every devnet node logs the verify-only client and never `initializing cpu prover`; `verifier_init_ms` < 10 000 and RSS < 1.5 GiB on all four, first log line → `RPC listening` < 60 s; demo-shielded under the light client (mint + two CORE-proof spends commit; a nullifier replay refused at admission — it never reaches a block — and a forged proof refused by consensus with a receipt); demo-agg under the light client (three COMPRESSED spends in ONE aggregate STARK, "Aggregate STARK verified" on every node — the recursion-vk-against-pinned-root check, live — plus the per-proof fallback); steady-state RSS printed per node; node0 killed and restarted on its persisted home: RPC back < 30 s, init < 10 s, keeps pace with node1 within 60 s.
- **Roll (2026-09-06 09:54–10:03 UTC, `ops/roll-v0.17.0.sh` from Cloud Shell) — THE R11 RECEIPT, per seat (systemd `MemoryCurrent` before → `hk_chainInfo.process` after; start = systemd `ActiveEnterTimestamp`, RPC = the `RPC listening` journal line):** val-1 **6,727 MiB → 59 MiB** (uptime 822 s), verifier init **795 ms**, start → RPC **6 s**, VOTING 32132 → 32108 @ 100991 → 101002 · val-0 **6,215 MiB → 56 MiB**, init **1,168 ms**, start → RPC **15 s**, VOTING 21492 → 21469 @ 101040 → 101050 · val-2 **6,457 MiB → 54 MiB**, init **1,011 ms**, start → RPC **13 s**, VOTING 10983 → 10962 @ 101082 → 101090 · gateway **6,088 MiB → 58 MiB**, init **1,401 ms**, start → RPC **16 s** from the start that took (see below), VOTING 31407 → 31383 @ 101106 → 101117, faucet back with 3 cooldowns restored and 50,506 drips left. Every seat restarted from its SEALED key through `LoadCredential`; the chain never paused (100972 → 101208 during the roll), quorum 5 of 6 held with both external seats up. Public RPC after the gateway: `node_version v0.17.0`, `process {rss ≈ 58 MiB, verifier_init_ms 1401}`, peers 6 (three founding + three external nodes on v0.16.0 / v0.16.1 / v0.16.0), `hk_getPoolPath` answers, Origin gate 403, faucet `/health` ok. **Restart-to-RPC on the fleet went from 3–6 minutes to 6–16 seconds and the resident set from ~6.4 GB to ~57 MiB** — the RAM line in the onboarding doc is lowered in this same commit (below). One wrinkle, handled by K5 exactly as designed: the gateway's first two starts (10:02:17, 10:02:28) were REFUSED — `SP1 verifier init FAILED … HTTP 502` — because the fleet still fetched the verifying keys from the internal prover proxy at start and the proxy answered 502 for ~20 s; systemd retried and the third start (10:02:38) wired the verifier normally. Fix applied the same hour, fleet-wide: the kit's `networks/testnet-1/vks.json` (sha256 `b7776b06…`) now sits at `/home/yadu/hk-validator/vks.json` on all four seats, so a restart reads the keys from the file (K6) and never depends on a prover being up — the founding seats now boot the way the onboarding doc tells an external operator to.
- **The RAM line, lowered by measurement:** `docs/VALIDATOR-ONBOARDING.md` §0, the validators kit, README-PUBLIC and the site now say **4 GB RAM minimum, 8 GB comfortable** (measured: ~57 MiB resident steady on the fleet minutes after start, ~300 MiB per node on a devnet while verifying mints, spends and a compressed aggregate; the rest is headroom for catch-up verification). Re-measured in a month; `docs/CAPACITY-SHEET.md` §g keeps the rows.
- **Ops:** `ops/roll-v0.17.0.sh` gains `memory <host>` (the process block + systemd `MemoryCurrent`) and prints init/RSS the moment a rolled host answers — the roll is the measurement; `ops/RELEASE-NOTES-v0.17.0.md` step 6 sequences the RAM-line change after the fleet numbers.
- **Docs:** `RPC.md` (`process` block), `VALIDATOR-ONBOARDING.md` §1 (RAM line re-measured, pending), `INCIDENTS.md` #6 (R11 shipped), `CAPACITY-SHEET.md`, `MASTER-BUILD-PLAN.md` / `C-PROGRAM-PLAN.md` / `P3.2-IMPLEMENTATION-PLAN.md` (R11 ✅).
- Unchanged: storage, keys, wallet (v0.14.1), consensus. No new dependencies.

## [0.16.1] — 2026-09-06 — 📜 H3: a paged pool feed, one-path spends, an incremental wallet scan (client-only; wallet v0.14.1)
- **H3 — `hk_getPoolNotes` / `hk_getPoolLeaves` are paged (`rpc.rs`).** `{from?, limit?}` → `{…, from, count, total, next}`; `limit` defaults to and is capped at **10,000** (`POOL_PAGE_MAX`, ≈24 MB of hex — bounded on the node whatever the pool grows to); `next` is the index the following page starts at, `null` at the end. A pre-H3 client that sends `{}` still gets the first 10,000 — the whole pool for a long time yet. Unit test `h3_pool_pages_are_bounded_and_chain_to_the_end`.
- **`hk_getPoolPath {index}` (new).** `{index, commitment, siblings[32], root, total}` from the node's leaf list (`hk_state::pool::full_tree_path`): a spender fetches ONE authentication path instead of every commitment. Trust boundary, plainly: the wallet re-folds the siblings and refuses a path that does not reach the stated root (`hk_wallet::fold_path`, `build_spend_with_path`, `build_disclosure_with_path` — unit-tested byte-identical to the leaf-list builders and refusing a bad root / wrong index / short path); the proof binds that root and the chain accepts it only while it is a recent anchor, so a lying node can cost a rejected tx, never a coin. `hk-node pool-path <RPC> <INDEX>` is the one-line diagnostic (folds locally, compares with `hk_getPoolInfo.root`).
- **Wallet v0.14.1 — incremental scan (`hk-wallet-gui/src/shielded.rs`).** `shield.json` gains a `scan` cache (`chain_id`, `scanned_through`, `notes[]` with value/rho/rcm/memo/spent) — sealed with the file when the wallet is protected, serde-defaulted for older files (one full scan, then remembered). A scan fetches only entries ≥ cursor (2,000 per page, 90 s budget), trial-decrypts them under every epoch, persists what it found, and asks `hk_nullifierSpent` only for notes not yet known spent; a different chain id or a pool shorter than the cursor starts over. Shield/unshield/pay/disclose build from the cache + `hk_getPoolPath` (fallback to a paged `hk_getPoolLeaves` on a pre-H3 node). The ACTIVITY line says how many new entries a scan read.
- **CLI wallet** (`hk-node wallet …`) reads the feed page by page (whole-feed reader, unchanged semantics).
- **Gate (receipt 2026-09-06: `gate-h3.sh` 22/22 GREEN, `gate-n1.sh` 20/20, rehearsal GREEN on the shipped binary `f6bfaf71…`; unit: hk-node keys ×4 + h3 ×1 + store ×3):** `chain/gate-h3.sh` (needs hk-prove): three shielded notes → every paging shape (`{}`, `limit 2`, `from 2`, `from 99`, `limit 0`), leaves pages concatenate to the whole, `hk_getPoolPath` folds to `hk_getPoolInfo.root` and errors out of range, a CLI unshield against the paged node appends two commitments, `{from:3}` yields exactly them, the newest leaf's path folds to the new root, scan shows 3 LIVE · 1 SPENT.
- **Docs:** `RPC.md` (paging, `hk_getPoolPath`, limits paragraph), `WALLET-GUIDE.md` (v0.14.1 scan, two troubleshooting rows).
- **Roll (2026-09-06 07:35–08:00 UTC, `ops/roll-v0.16.1.sh` from Cloud Shell):** release assets verified against `networks/testnet-1/CHECKSUMS` by GitHub's own digests (gz `710708b1…`, wallet `17566a9e…`, Linux zip `8c9abbb0…`); binary `f6bfaf71…` sha-checked on all four hosts before any start; K5 precheck green; rolled val-1 → val-0 → val-2 → gateway (+faucet, +bot), one voter at a time, **each restarting from its SEALED key through `LoadCredential` and proven VOTING before the next** (val-1 12311 → 12307 @ 98667 → 98725 · val-0 26672 → 26654 @ 98825 → 98833 · val-2 16006 → 15986 @ 98884 → 98932 · gateway 11259 → 11240 @ 99026 → 99034); the chain never paused (98665 → 99086 during the roll) with both external seats up (quorum 5 of 6 held throughout). Public RPC after the gateway: `node_version v0.16.1`, `hk_getPoolNotes {limit:1}` → count 1 / total 20 / next 1, `hk_getPoolPath {index:0}` → 32 siblings, root `09bea259…` = `hk_getPoolInfo.root`; faucet `/health` drips_left 50,506, low false.
- Also in this release (from the K2/K3 fleet work the same night):
  - **`key-seal` / `account-seal` take `<PREFIX>_PASSPHRASE_FILE` for a NEW seal** (first line, trimmed, same strength rule) — the fleet pattern is now one file end to end: `passphrase-new > file` → seal from the file → `install` the same file as the systemd credential; nothing in an environment variable or a `$(cat …)` on a command line (`keys.rs`; test `k2_new_seal_takes_the_passphrase_from_a_file`, keys ×4).
  - **An unknown command exits 2** (it used to print the usage and exit 0 — `hk-node account-unseal DIR && shred -u passfile` on an older binary shredded the passphrase file during the K3 migration; the file had a second copy, nothing was lost). Bare `hk-node` still prints the usage and exits 0.
  - **No more EPIPE panic:** `hk-node … | head -1` ended in `failed printing to stdout: Broken pipe` + an `unwrap` panic in `main` (seen on the gateway during the K3 migration). `main()` now restores `SIGPIPE`'s default disposition on unix (`libc = "0.2"` under `[target.'cfg(unix)'.dependencies]`, already in the lock file transitively).
  - **CI:** `wallet-linux.yml` on `actions/checkout@v5` + `actions/upload-artifact@v5` (the Node 20 deprecation warning from run #1). The Linux wallet artifact is to be attached to the v0.16.1 release before publishing.
  - **Docs:** `VALIDATOR-ONBOARDING.md` §5a and `FAUCET-RUNBOOK.md` §2 rewritten to the generate-into-file pattern used on testnet-1's own seats and faucet, with the honest limit spelled out: on a cloud VM the credential file and the sealed file share a disk — the seal defends against home-directory backups, transplant tars and service-user reads, not against a full disk image (the HSM/KMS seam in `MAINNET-KEY-MANAGEMENT.md` is the real separation); keep an off-host copy of the credential — a lost passphrase is a lost seat.

## [0.16.0] — 2026-09-05 — 🗄️🔐 S+K: storage that stops growing in the wrong places, keys that can live sealed — block-log segments + retention, persisted search index, snapshot knobs; `HKE1` sealed key files (validator seed, account/wallet files, GUI), faucet hot/cold + low-watermark (client-only; no consensus change, no activation; wallet v0.14.0)
- **C2.8 — block-log segments (`store.rs`).** Committed blocks keep landing as one file per height (atomic, crash-safe — the hot tail); at snapshot time, every completed run of 1,024 heights at-or-below the snapshot is packed off the commit path into `blocks/seg<id:09>.hkb` (`"HKS1" | frames(u32 len + bincode StoredBlock) | index[1024] u64 offsets (0 = absent) | u64 index_offset | "HKS1"`, tmp → fsync → rename, then the per-height files go). Readers try the per-height file first, then the segment; a segment is written before anything is deleted, so a reader never sees a gap. One compaction at a time (a lock); a node upgraded from ≤ v0.15 packs its history in a background `hk-compact` thread on first start (`HK_COMPACT_HISTORY=0` skips it). testnet-1 at height ~76k: ~76,000 files → ~74 segments + the tail. Unit tests: roundtrip + idempotence, retention on whole segments only.
- **Retention (`HK_RETAIN_BLOCKS=N`)** deletes whole segments older than `tip − N` — never the hot tail, never a partial segment; the servable floor (`hk_chainInfo.history.disk_from`) moves up and `history.retain_blocks` says N (null on an archive node). Voters and the public gateway keep everything; the knob exists for observers and for the day a disk is small.
- **S2 — persisted search index (`index3.bin`).** The tx and account indexes (`PersistedIndex{through_height, tx[], acct[]}`) are written at snapshot time and every `HK_INDEX_PERSIST_EVERY` blocks; a restart loads them and resumes the index pass **above** `through_height` instead of re-reading every block file (the `index_floor` marks what is still being back-filled; `u64::MAX` once the history pass is done). Log: `search index persisted (S2)` / `search index restored from disk (S2) — resuming above it`.
- **S3 — snapshot knobs.** `HK_KEEP_PREV_SNAPSHOT=1` keeps `snapshot3.prev.bin` (an operator's escape hatch if the newest image ever fails its commitment check); `HK_SNAPSHOT_EVERY` (existing), `HK_INDEX_PERSIST_EVERY`, `HK_COMPACT_HISTORY` — all documented in `docs/VALIDATOR-ONBOARDING.md` §5b.
- **K1/K2 — keys at rest (`hk-wallet/src/sealed.rs`, `hk-node/src/keys.rs`).** One envelope for every secret file: `HKE1` = Argon2id → XChaCha20-Poly1305, AAD `hk/v1/sealed`, one salt per key and a fresh nonce per write, a JSON envelope that says what it is (`hke, kdf, m_kib, t, p, salt, nonce, ct[, kf]`) and reveals nothing else. Plain files keep working — the loaders sniff the envelope, never ask for anything on a plain file. Passphrase sources, first hit wins: `<PREFIX>_PASSPHRASE` → `<PREFIX>_PASSPHRASE_FILE` (first line) → systemd `$CREDENTIALS_DIRECTORY/<credential>` (`LoadCredential=`) → a no-echo prompt when stdin is a terminal; otherwise a loud refusal (`this file is sealed — set …`) rather than a half-armed node. A wrong passphrase fails the AEAD tag — no partial decrypt, a clean `wrong … passphrase` error.
  - **Hardened against offline guessing (the question "is it brute-force hardened?", answered with the QE-Vault profile brought to a size a validator host can afford):** default **Argon2id 512 MiB × 4 passes × 4 lanes (≈1 s)** — memory-hard, so a 24 GB GPU fits ~48 guesses at a time and manages tens of guesses per second, not billions; parameters ride in the envelope (`HK_SEAL_M_KIB` / `HK_SEAL_T` for new seals, floor 64 MiB / t=3). **The KDF runs once per unlock**, the derived key is cached per salt (`SealKey`), so reserve-then-sign re-seals are an AEAD call — a heavy KDF costs nothing per send. **Passphrases are checked, not trusted** (`check_strength`: ≥ 12 characters or a 4+-word phrase; common words with decoration, keyboard walks, repeats and digit runs refused; `HK_SEAL_ALLOW_WEAK=1` only for devnets); `hk-node passphrase-new` and the GUI's *generate* print 7 words from a 512-word list (63 bits — millions of GPU-years at this cost). **Optional key file, a second factor:** `hk-node keyfile-new PATH` + `HK_KEY_KEYFILE` / `HK_WALLET_KEYFILE` mix 32 random bytes into the key; the envelope names the file's fingerprint (`kf`), and without the file the passphrase opens nothing — brute force becomes impossible rather than slow. Post-quantum note: a 256-bit symmetric key under Argon2id is already quantum-safe; no lattice KEM is needed for a local file.
  - **K2 validator seed:** `hk-node key-seal | key-unseal <HOME>` on `priv_validator_key.json` (prefix `HK_KEY`, credential `hk-key-passphrase`); `start`, `issue-rotation`, `set-change approve` read the sealed file; `keygen` seals from the first byte when `HK_KEY_PASSPHRASE` is exported. Onboarding §5a + the unit's `LoadCredential=hk-key-passphrase:/etc/hk/key-passphrase`. Honest scope: a running node holds the seed in memory, and `consensus_state.bin` (the live LMS tree state, rewritten per vote) stays plain in this release — the current operational tree only; a root-signed rotation retires it. `docs/MAINNET-KEY-MANAGEMENT.md` names the HSM seam (`HashSigner::sign` / `RootSecret::sign`) this plugs into.
  - **K1 wallet files:** `hk-node account-seal | account-unseal <DIR>` seals `account.json` + `wallet.json` (+ `shield.json`) under one passphrase and one salt (prefix `HK_WALLET`, credential `hk-wallet-passphrase`); `account-*`, `wallet …`, `asset …` and `faucet-serve` read them; reserve-then-sign re-seals on every save with the cached key (microseconds). Secret files are written tmp → fsync → rename, mode 0600 on Unix.
  - **Wallet v0.14.0 (GUI):** *Backup & advanced → Protect with a passphrase* seals `account.json` and `shield.json` with the same envelope (CLI and GUI still share a wallet); the same strength rule, a *generate* button (7 words, shown once, copy), `HK_WALLET_KEYFILE` honoured; a sealed wallet opens to an **Unlock** screen (≈1 s, once) instead of offering to create a wallet over it; change / remove passphrase re-writes both files only after reading them back under the old one; the derived key lives in memory for the session. `docs/WALLET-GUIDE.md` §5a.
- **K3 — faucet hot/cold + low-watermark (`faucet.rs`, `docs/FAUCET-RUNBOOK.md`).** The faucet is now run as a hot wallet with a small float, refilled by `account-send` from a cold treasury that never sits on the faucet host (two accounts, two signers — the "stop the faucet before a treasury send" rule is gone). `/health` gains `low` (balance < `HK_FAUCET_LOW_MICRO` / `--low-micro`, default 50 drips), `low_watermark_micro`, `reserve_micro`, `drips_left`; below `HK_FAUCET_RESERVE_MICRO` (default 2 drips) a drip is answered **503 "faucet is being refilled"** before any nonce is reserved — no ratchet index is burned on a transfer the chain would refuse. Startup and every low drip log the refill warning.
- **RPC:** `hk_chainInfo.history.retain_blocks`. No new methods; `hk_getBlock` on a pruned height answers not-found as before.
- **Gates (receipt: `gate-s.sh` 33/33 GREEN, `gate-n1.sh` 20/20, rehearsal GREEN on the shipped binary; shas in `networks/testnet-1/CHECKSUMS`):** `chain/gate-s.sh` — on a 4-validator devnet with the S3 knobs: `index3.bin` + `snapshot3.prev.bin` written, `retain_blocks` null on an archive node, a restart restores the index from disk and keeps deciding; an observer with `HK_RETAIN_BLOCKS=64` reports it; `key-seal` → start without a passphrase refuses, with a wrong one refuses cleanly, with `HK_KEY_PASSPHRASE_FILE` the seat **votes** (remaining falls while height rises), `issue-rotation` reads the sealed key, `key-unseal` restores the byte-identical plaintext; `account-seal` → `account-info` refuses without / reads with / refuses wrong, `account-send` from the sealed directory is accepted and the file is still sealed with the nonce advanced; the faucet on the sealed directory: `/health` watermark fields, 503 below the reserve with the nonce untouched, 200 with a txid above it. `--long` (≈25 min) waits for the first segment and reads block 1 from it. `gate-n1.sh` now takes the version tag from the binary. Unit tests: `sealed` ×7 (roundtrip + cached-key re-seal, key file as a second factor, envelope detection, passphrase-source order, strength rule, generator, profile floors), `keys` ×3 (seal/read/write/unseal cycle with no second KDF, non-JSON + weak passphrase refused, key file through the env), `store` (C2.8 ×2, S2). Gate additions: weak passphrase refused with the file untouched, salt unchanged across a save, key file names itself and refuses the passphrase alone.
- **Deps (hk-wallet):** `argon2 0.5`, `chacha20poly1305 0.10` and `zeroize 1` (already in the tree), `rpassword 7`, `rand 0.8`. Fleet roll: not required for consensus (client-only); rolled for storage hygiene (the gateway's 76k per-height files) with `ops/roll-v0.16.0.sh`; the K3 hot/cold split is a one-time ops procedure after the roll. `hk-node --version` → v0.16.0.
- **Roll (2026-09-05 14:49–15:30 UTC, `ops/roll-v0.16.0.sh` from Cloud Shell; the driver and the gz went up through Cloud Shell's upload menu this time):** binary `e4af8aef…` verified on all four hosts before any start; K5 precheck green (unit prover + public prover from every host); rolled val-1 → val-0 → val-2 → gateway (+faucet, +bot) one voter at a time, each proven VOTING before the next (val-1 28043 → 28041 @ 80757 → 80811, then 27992 @ 80831 · val-0 12085 → 12068 @ 80869 → 80942 · val-2 30666 → 30646 @ 81022 → 81029 · gateway 26696 → 26678 @ 81107 → 81114); the chain never paused (80745 → 81176 during the roll); the external seat up throughout (quorum 4/5). **The storage migration on real 81k-block homes:** val-1 had 70 segments 5 min after its start; val-0 logged `block log: migration to segments complete segments=78` 7 min after start and then packed segment 78 at the next snapshot (`segment compacted segment=78 files=1024`) — **248 per-height files + 79 segments** where there were ~81,000 files; val-2 the same (255 + 79); the gateway was at 49 segments / 30,957 files 3 min in and still packing. `index3.bin` on every host. Verified from outside: `hk_chainInfo` node_version v0.16.0, `history.retain_blocks` null (archive), `disk_from` 1, `hk_getBlock 1` served, validators 5, `hk_getAssets` empty, Origin gate 403, faucet `/health` with `low: false` and `drips_left`; `hk_getPeers` on the gateway: 6 peers — 3 fleet outbound v0.16.0 and **three external inbound nodes** (37.60.237.0/24 ≤ v0.15.1 = the first external seat, 101.228.67.0/24 ≤ v0.15.1, and a new 43.155.163.0/24 on v0.15.2 — a third unannounced external node). Every seat runs v0.16.0; keys stay plain until the per-host `key-seal` pass and the K3 hot/cold split (next).

## [0.15.2] — 2026-09-05 — 📡 N1: the live peer table — `hk_getPeers`, `node_version`, peers advertise their version; the /network page shows who is on testnet-1 in real time (client-only; no consensus change, no activation)
- **Asked for as "something live on the website that scans how many external nodes there are"; built as a measurement, not a proxy.** The node's own swarm loop learns everything worth showing — who is connected, which way, from which address, on which genesis, on which version — so the vendored network crate now keeps a process-wide **peer table** (`HkPeer`: peer id, `inbound|outbound`, the connection's real remote address, identify status, genesis tag, node version, connected-since, live connection count), updated on `ConnectionEstablished`, on identify (consensus protocol, after the genesis gate) and on `ConnectionClosed` (the entry is removed when the last connection goes). An entry exists only while a socket is open: joins appear, leaves vanish, nothing is configured.
- **`hk_getPeers`** (read-only, browser-callable): `self{peer_id, version, genesis_digest}`, `count`, `inbound`, `outbound`, `public_addr`, `identified`, `islands_refused`, `peers[…]`. The node masks addresses itself — /24 for v4, /48 for v6 (v4-mapped v6 masked like v4), DNS kept — and flags `private_addr` (loopback / RFC 1918 / CGNAT / ULA: the founding fleet peers over its private network). `hk_chainInfo` gains `node_version` (the v0.14.1 nit: operators can finally prove which binary answers) and `peers`.
- **Version tag.** identify's `agent_version` becomes `hashkinetics/1/genesis/<64 hex>/hk-node/<version>`. The genesis-gate parser now takes the first 64 hex chars and accepts `/…` after them, so ≥ v0.15.2 gates both generations; a ≤ v0.15.1 node reads the new tag as "no HK tag" and GRACES the peer (fail-open — the gate is defense in depth, never consensus) until it upgrades. **Island chains are now counted** (`HK_ISLANDS_REFUSED`): a peer on a different genesis is disconnected as before and no longer appears anywhere as a peer.
- **Website.** `/network#live` — "The roll call, measured": voting seats (founding vs external, from `hk_getValidators` against the four ceremony addresses), external nodes (identified peers on a public address), node versions seen, island chains refused, one row per external peer (masked address, direction, version, genesis, connected-for), the founding fleet as one line, the gateway's own id — polled every 10 s from `rpc.hashkinetics.org`; degrades to "soon" while the gateway still runs ≤ v0.15.1. The page's join flow now tells the v0.15.1 truth (copy `vks.json`; no `HK_PROVER_URL`); `facts.json` claim `n1-live-peer-table`, `validator_set` and "how to verify" refreshed; `llms.txt`.
- **Gate:** `chain/gate-n1.sh` — 4-validator devnet: node0 sees 3 identified peers, all `v0.15.2` · `match` · masked loopback; `hk_chainInfo.node_version`; a 5th node joins → inbound on node0 within seconds, outbound on itself, `connected_secs` climbs, syncs from genesis; it stops → node0 back to 3, no stale entry; an island (fresh genesis, same binary) dials node0 → `islands_refused ≥ 1`, identified peers unchanged, the island sees 0 identified peers and refuses us too; 4/4 tables name exactly the other three self ids. Unit tests: masking (public/private v4, v6, v4-mapped, DNS, garbage) and both tag generations.
- **`hk_getBlock.set_changes[]` / `hk_getBlocks[].set_changes`** — the validator-set change certificates a block carries (`change: admit|remove`, `root_pk`, `voting_power`, `approvals`, window). Found missing the moment it mattered: block 72219 seated testnet-1's first external validator and the explorer surface could not show it.
- **Testnet-1 receipt — the second external voting seat (2026-09-06 ~05:40 UTC).** Operator "679 WUMP" (Linux VPS, Asia; Discord + e-mail cross-checked, byte-identical `validator.json`, public halves only) posted `hk-node v0.15.2`, height 93197, app_hash `afab9e6d…` — equal to block 93198's `parent_app_hash` on the public RPC — and his consensus multiaddr. Certificate approved by **4 of 5 seats** (SLH-DSA-192s roots 3fc0cf82…, 0dc77ed1…, eff24e00…, b2709fad… — **each signing from a SEALED `priv_validator_key.json`**, passphrase piped from the root-only credential, the first ceremony on sealed keys), window 96590–101540, committed in block **96629**, effective **96630**: validator set 5 → 6, power 6 — `97E166933F4DF6CA762B962C3CFA0F8DBC177542` (root 39198497…) joins 175D9A8E…, 42C00CD6…, E77F5F47…, F7D75524…, F874765D…. No new genesis, chain never paused; commit certificates carry 5 signatures from 96630 (quorum is now 5 of 6; one absent seat is survivable). Driven from Cloud Shell with `ops/admit-seat.sh` on v0.16.0 binaries; the operator runs v0.15.2. Two external seats hold 2 of 6 power.
- **Testnet-1 receipt — the first external voting seat (2026-09-05 07:06:37 UTC).** Certificate approved by 3 of 4 seats (SLH-DSA-192s roots 3fc0cf82…, 0dc77ed1…, eff24e00…), committed in block 72219, effective 72220: validator set 4 → 5, power 5 — 175D9A8EA5934E2AE3E267AC62C91751E6663D87 (external operator, root 07934e7d…) joins 42C00CD6…, E77F5F47…, F7D75524…, F874765D…. No new genesis, chain never paused; the new seat proposed and voted from 72220 (a few round-1s from WAN latency, block cadence unchanged at ~3 s). Driven from Cloud Shell with `ops/admit-seat.sh` (check → propose → approve → submit → watch) on v0.15.0 binaries; the operator ran v0.15.1. Quorum is now 4 of 5.
- **Release receipt:** build clean (3 pre-existing warnings); `cargo test -p hk-node` 22 (the two `n1_` tests: masking, both agent-tag generations); rehearsal gate GREEN on the shipped binary (sha256 df66ca57ae0db8fc22d6e8d358dbc55a56ae8e09fb91857e87273c29840f2d61; `hk-node-v0.15.2.gz` 91876bebce06feeeffd330871c48d6cd55a57ee8d101ac406ed76e1b32c18230). First gate-n1 run: 16/20 with the four misses all in the harness (tuple-formatted expectations against space-separated prints; the node's answers were right on every one) — fixed; reruns 19/20 (one timing miss) then **20/20 GREEN** (waits lengthened afterwards).
- Fleet roll: not required for consensus (client-only); rolled for hygiene — this roll also carries v0.15.1 for the seats still on v0.15.0. `hk-node --version` → v0.15.2. Vendored-Malachite patch surface: `vendor/external/malachite/code/crates/network/src/lib.rs` only (HK genesis-gate block + HK peer-table block).

## [0.15.1] — 2026-09-05 — 🔑 K6: the join kit works on the stock binary — verifying keys ship in the kit, https client (client-only; no consensus change, no activation)
- **Found by the first external operator (2026-09-05, `docs/INCIDENTS.md` #10):** the node's prover client (`verifier.rs`) and the CLI's RPC client (`demo.rs`) were plain HTTP over a raw `TcpStream`, stripping only `http://` — `HK_PROVER_URL=https://prover.hashkinetics.org` (the kit's own instruction) and `account-send https://rpc.hashkinetics.org …` (the README's) could not work on the stock binary; with K5 that meant a node following the kit **refused to start**. The fleet never saw it (internal `http://` prover). He patched https in locally, resynced from genesis and matched our `app_hash` at H58837 hash-for-hash — the first independent re-verification of testnet-1.
- **Verifying keys in the kit.** `networks/testnet-1/vks.json` carries the three SP1 verifying keys (hex bincode, 104 bytes each). A node reads `HK_VKS_FILE=<path>`, else `<HOME>/vks.json`, else fetches from `HK_PROVER_URL`; every path checks the keys against the genesis `vk_pins` and refuses on mismatch. **A node on a pinned genesis needs no prover to verify** — the live dependency the kit README had ledgered as "queued" since v0.10.9 is gone. `hk-node vks-fetch <PROVER_URL> [-o vks.json] [--genesis genesis.json]` writes the file (and refuses to write on a pin mismatch).
- **One https-capable HTTP client** (`demo::post_json`, ureq + rustls) behind the prover fetch, `genesis-build`'s pin fetch and every CLI command; a bare `host:port` still means http. K5's refusal message now names both fixes (`vks.json` / `HK_VKS_FILE` or `HK_PROVER_URL`).
- **Docs:** `networks/testnet-1/README.md` (copy `vks.json` next to the genesis; no env needed), `docs/VALIDATOR-ONBOARDING.md` §5 + the systemd unit, `README-PUBLIC.md`. **Gate:** `chain/gate-k6.sh` — `vks-fetch` matches the devnet pins; a validator restarted with `HK_VKS_FILE` and no prover URL rejoins and votes; the default `<HOME>/vks.json` path is picked up; a tampered file is refused (`PIN MISMATCH`, K5 names `HK_VKS_FILE`); `account-balance` and `vks-fetch` against the public https endpoints succeed from the stock binary. **Gate receipt (2026-09-05 ~03:00 IST, WSL): build clean, `gate-k6.sh` 16/16 GREEN (node3 verified from the file and voted 32,746 → 32,714; tampered file refused; https RPC answered the treasury balance; the kit's `vks.json` equals the public prover's keys), rehearsal GREEN on the shipped binary.** `hk-node-v0.15.1.gz` sha256 `85efc5edb3284cd163524006bb8ca6198d3787b7dc22f9c4e0c2e3677118bf35` → `hk-node-v0.15.1` sha256 `61a9a1420dda2d5353809bc6726fa1e923581efdbaeb73bfcc8b0bd05337fc9c`; `networks/testnet-1/vks.json` sha256 `b7776b06547f238ac78ef8f15ab9af9cb9c9919094c9f441e506afdd4a9185b9`.
- Fleet roll: not required for consensus (client-only); rolled for hygiene when convenient. `hk-node --version` → v0.15.1.

## [0.15.0] — 2026-09-04 — 🪙 X1: issued assets — registry + issuer mint · burn · freeze · pause — ROLLED to all four testnet-1 hosts 19:20–19:50 UTC (state-compatible; activation at the first asset transaction)
- **Why:** an issuer (Circle's xReserve partner flow, Tether, any regulated stablecoin) needs mint/burn/freeze/pause and a provable supply on day one; until now the only supply source was the genesis `alloc` table and the fee asset was a hard-coded constant (`docs/STABLECOIN-RAILS-AND-ORACLE-PLAN.md` §X1). Design: `docs/X1-ISSUED-ASSETS.md`.
- **Registry (`hk-state/src/assets.rs`):** `AssetRegister { asset, symbol, decimals, policy }` — the sender becomes the issuer and `asset` must equal `H("hk/v1/asset-id" ‖ issuer ‖ symbol)` (squat-proof like account ids; two issuers' `USDC` are two assets). Policy fixed at registration: `mintable / freezable / pausable / pool_eligible`. `AssetMint` (issuer), `AssetBurn { destination ≤ 64 B }` (holder), `AssetFreeze { account }`, `AssetPause` — each a receipt string the explorer shows (`frozen by issuer`, `asset paused`, `sender is not the asset's issuer`, `asset policy forbids this`, `asset is not pool-eligible`).
- **One gate on every movement of a registered asset:** `Transfer`, the funding leg of `AccountCreate`, `MandateSpend` (the ROOT funder pays), `ChannelOpen` (escrow leaves the funder), `ChannelSettle`, `ChannelRefund`, `MintToPool` (+ `pool_eligible`), the unshield credit of `ShieldedSpend`, mint, burn. Unregistered assets — testnet-1's native asset — are untouched, byte for byte. The protocol fee leg is not issuer-gated; a genesis that registers its fee asset as pausable/freezable is refused at load.
- **Supply accounting in Σ:** per-asset `supply` / `burned` / `paused` / frozen set enter the state commitment **only once the registry is non-empty** (tag `0xA5`) — v0.14 and v0.15 nodes agree on every block until the first registration commits, so the roll cannot fork (the v0.12 fee-counter trick). Conservation invariant **I5'** (`Σ balances + Σ open escrow + pool = supply − burned − fees_burned`) is tested and exposed as `hk_getAsset.conserved`.
- **Genesis:** `chain.assets[{id, symbol, decimals, issuer, policy}]` and `chain.fee.asset` (absent on testnet-1 → its bytes and chain id are unchanged; `FEE_ASSET` stays the default). `hk-node genesis-build --asset SYMBOL:DECIMALS:ISSUER-AUTH0:FLAGS[:ID] --fee-asset HEX`; the state machine validates the file before it is written.
- **Snapshot v3:** `StateSnapshot.assets` (last field); the node writes `snapshot3.bin`, reads `snapshot2.bin`/`snapshot.bin` (highest height wins); a v2 file restores with an empty registry and the app_hash check decides.
- **Wire:** five `Tx` + five `Event` variants appended LAST (`x1_wire_variants_are_appended_last` pins the bincode tags 10–14). **Activation rule as for `AccountCreate`/V1: every node ≥ v0.15.0 before the first asset transaction commits** — a v0.14 node halts at that block by design. Minimum node version for testnet-1 becomes v0.15.0 at that moment.
- **Surface:** RPC `hk_getAsset {asset | issuer+symbol}`, `hk_getAssets`, `hk_getAccount.balances[]` by asset; block/tx summaries `asset_register|mint|burn|freeze|pause`; CLI `hk-node asset-id`, `hk-node asset register|mint|burn|freeze|unfreeze|pause|unpause|info|list` (signed through the self-custody reserve-then-sign path). `docs/RPC.md` updated.
- **Release (2026-09-04, UTC evening / 05 IST):** rehearsal gate GREEN on the exact binary (R10 v2 shapes A–D, HK_FEE_FROM override refused, observer from genesis via disk-served history, RSS ~6.8 GB/validator as before); `hk-node-v0.15.0.gz` sha256 `4187a31f63ee2941a84e508f4cdb85c16b2e713b259665f54d6095ba7a67b133` → unpacked `hk-node-v0.15.0` sha256 `38fb156b057314d7176b02b946ff7542ca469742df71fe050c64c1c9b6aca5b4`; GitHub release `v0.15.0` (immutable, node .gz + wallet v0.13.1 .exe); `networks/testnet-1/CHECKSUMS`, `facts.json`, `llms.txt`, site strings, yellowpaper/whitepaper addenda, audit scope, onboarding and all nine decks re-stamped v0.15.0 in the same push.
- **Roll (2026-09-04 19:20–19:50 UTC, `ops/roll-v0.15.0.sh` from Cloud Shell):** binary `38fb156b…` verified on all four hosts before any start; K5 precheck: both the unit's prover and the public prover answered from every host; rolled val-1 → val-0 → val-2 → gateway (+faucet, +bot) one voter at a time, each proven VOTING before the next (val-1 27090 → 27072 @ 59024 → 59032 · val-0 6493 → 6479 @ 59054 → 59133 · val-2 28484 → 28483 @ 59157 → 59216 · gateway 26012 → 25992 @ 59316 → 59325); the chain never paused; faucet back with cooldowns restored. Verified from outside: `hk_getAssets` on the public RPC → `{assets: [], count: 0, fee_asset: 0909…09}` (the registry is live and empty), `hk_getAccount.balances[]` lists the treasury's native balance, `hk_getValidators` count 4 with `pending_set_changes`, Origin-gated operator method → 403, faucet `/health` ok. Every seat runs v0.15.0; the first asset transaction on testnet-1 is now a deliberate act (after the external operator confirms his version), and it makes v0.15.0 the minimum from that block.
- **Gate receipt (2026-09-04 evening, WSL): build clean (3 pre-existing warnings), hk-state 21/21 incl. `x1_register_mint_freeze_pause_burn_storyline`, `x1_wire_variants_are_appended_last`, `x1_genesis_assets_fee_asset_and_snapshot_v3`, `x1_gates_reach_mandates_channels_and_the_pool`; `chain/gate-x1.sh` 40/40 GREEN** — on a fresh 4-validator devnet: registry empty at start → `org` registered `USDC.t` (found by id and by symbol@issuer, policy `mfps`) and a second issuer's `USDC.t` got a different id → mint 5,000,000 to alice, `supply 5,000,000 conserved` on all four nodes, `hk_getAccount` listed the balance by symbol → alice→bob transfer, freeze bob: both directions `rejected: frozen by issuer` while bob's native asset still moved, mint to bob refused, unfreeze → moved → pause: transfer and mint `rejected: asset paused`, `paused=True` conserved, unpause → mint landed (supply 6,000,000) → alice could not mint org's asset (`not the asset's issuer`), burned 1,000,000 with destination `eth:0xabcd` → `supply 6,000,000 · burned 1,000,000 · conserved`, tx index kinds `asset_burn,asset_mint,transfer` → 4/4 same app_hash + registry → node1 restarted from `snapshot3.bin` (`Snapshot restored`) at the same app_hash → node4 synced from genesis (height 80) with the same app_hash and derived the 2-asset registry by replay. Two harness bugs fixed on the way (relative binary path after `cd`; Python sort on a `None` symbol) — chain behaviour needed no change. Deliberately out: attested mint (X2), policy/issuer changes, `hk_getBurn` + destination formats (X3), multi-asset pool, fees in another asset (X6).

## [0.14.0] — 2026-09-04 — 🪑 V1: validator-set changes on a running chain (devnet gate GREEN 25/25) — rolled to all four testnet-1 seats 2026-09-04
- **`SetChangeCert`** (hk-consensus `setchange.rs`): admit or remove ONE seat, approved by SLH-DSA root signatures from strictly more than ⅔ of the CURRENT seats' voting power, bound to the chain id and to a commit-height window `[not_before, not_after]`; idempotent; never empties the set. Verified at submit, at propose and at commit — by every node, against the set as it stands; effective for `height + 1`; recorded in the HK-R6 per-height history (sync across the boundary is free). Three unit tests (supermajority math, replay/window/chain refusals, last-seat + address-collision refusals).
- **Wire v2 for `Batch`:** `set_changes` rides the block; a batch WITHOUT a set change encodes byte-identical to the v1 wire (old blocks, old nodes untouched); one WITH a set change carries the `HK-BLK-2` magic. Activation rule as for `AccountCreate`: every node ≥ v0.14.0 before the first set change commits.
- Node: `pending_set_changes` queue (`hk_submitSetChange`, operator method, Origin-gated), proposer inclusion inside the window, `apply_set_changes` on commit + replay with `Validator ADMITTED / REMOVED` log lines (`THIS NODE now holds a voting seat`), `hk_getValidators.pending_set_changes`. CLI `hk-node set-change propose | approve | assemble` (approve runs on each seat's host — keys never move).
- Gate: `chain/gate-v1.sh` — 4-validator devnet → observer joins → 3-of-4 admit → it VOTES (leaves fall) → 2-of-5 and wrong-chain certs refused → 4-of-5 remove → an observer syncs from genesis across both boundaries.
- Docs: `docs/V1-VALIDATOR-SET-CHANGES.md` (rule, wire, testnet-1 runbook), `docs/RPC.md` row, P3.2 row.
- **Gate receipt (2026-09-04, WSL devnet, `chain/gate-v1.sh`): 25/25.** node4 joined as an observer at height 8 (app_hash equal, 32,768 leaves untouched) → 3-of-4 admitted it → 5 identical seats on every node → it VOTED (leaves 32,757 → 32,726 over heights 40 → 54) → a 2-of-5 cert refused (`approving power 2 is not > 2/3 of 5`) and a wrong-chain cert refused → 4-of-5 removed it → 4 seats everywhere, node4 kept syncing (72 → 86) spending nothing → node5 synced from genesis to 88 across both boundaries, derived the 4-seat set by replay, log shows both changes.
- **Roll (2026-09-04):** one voter at a time, each proven voting (its one-time-key counter falling while the height advanced) before the next, gateway last; the chain never paused (heights 55641 → 56023). `hk_getValidators` on the public RPC now carries `pending_set_changes`. Every testnet-1 seat runs v0.14.0, so the first set change may commit; the minimum node version for testnet-1 becomes v0.14.0 at that moment.
- **K5 — no silent refuse-all on a pinned network.** The first external operator (Eddy, 2026-09-04) wedged at height **479** — testnet-1's first shielded block — with `app_hash 329f0903…` vs the canonical `f19e9ad9…`: his node ran without `HK_PROVER_URL` (or started while the prover was unreachable), kept hk-state's RejectAll verifier, refused a valid proof and diverged, exactly the CHANGELOG 0.10.9 story. A node on a genesis with `vk_pins` now **refuses to start** without a wired verifier, with the fix in the error (`HK_ALLOW_UNVERIFIED=1` overrides for experiments). Shipping the verifying keys inside the join kit (no live prover dependency at startup) stays queued.

## [0.13.2] — 2026-09-03 (evening) — 🛡 node hardening for public exposure (H-batch) + R12 rotation jitter — ROLLED to all four testnet-1 hosts 15:55–16:22 UTC
- **R12** per-seat rotation threshold in 15–25% of capacity, derived from the seat's root key (`rotation_threshold`); `rotation_due` now takes the threshold; unit test guards distinctness and range. Finding it answers: all four testnet-1 seats rotated within eleven blocks on 2026-09-03.
- **H1** RPC: 10 s per-request timeout (408), 256-connection cap (503), body cap enforced before the read · **H2** operator methods (`hk_submitRotation`, `hk_gossipTxs`, `hk_submitBundle`) refused with 403 when the request carries an `Origin` header (`HK_RPC_ALLOW_BROWSER_OPS=1` for devnets) · **H4** bundle queue bounded (64) + de-duplicated · **H5** gossip call ≤ 1,024 txs · **H6** mempool WAL `fdatasync` per admission (`HK_WAL_FSYNC=0` for benches) · **H7** poison-proof locks throughout rpc.rs · **H8** no filesystem paths in RPC error strings · **H9** faucet trusts `X-Forwarded-For` only from a loopback/private peer, cooldowns persisted (`faucet-cooldowns.json`) and bounded (100k) · **H10** a genesis without a fee policy runs fee-free unless both `HK_FEE_*` are set (the 110,000 placeholder is gone) · **H11** `hk-node --version`, banner says the release; `hk-rpc` orphan crate excluded from the workspace · **H12** validator master seeds from `OsRng`.
- **Build + gate + roll (2026-09-03):** clean build, hk-node 19/19 + hk-state 14/14, rehearsal gate GREEN on the released binary (`b7601e84…`; `.gz` `b94b89f0…`). Rolled val-1 → val-0 → val-2 → gateway (+faucet, +bot) one voter at a time, each proven VOTING (one-time-key counter falling, height advancing) before the next; the chain never paused. Observed: val-1 answered RPC 12 s after restart, val-0/val-2/gateway took ~4–5 min (the proof-system verifier init — R11 evidence). Verified from a browser origin: `hk_submitRotation`/`hk_gossipTxs` → 403, `hk_getValidators` → 200, faucet `/health` → 200. All four seats were already at epoch 2 (a second rotation cycle happened during the day under the old 20% rule); the R12 spread shows at the third cycle. Release notes + roll driver: `ops/RELEASE-NOTES-v0.13.2.md`, `ops/roll-v0.13.2.sh`.

## [0.13.1] — 2026-09-03 — 🔁 the first rotation on testnet-1 · the sweep after launch · wallet v0.13.1 (wallet-only release; the node stays v0.13.0)
### Receipts (testnet-1, 2026-09-03)
- **All four seats rotated their signing trees unattended at 04:03 UTC** — rotation certificates at heights **11,584 · 11,588 · 11,594 · 11,595** (one per validator; block times 1788373803–1788373814), every seat now `epoch 1`, zero blocks missed, cadence ~19 blocks/min since launch. Finding: fresh trees started together cross the 20% threshold together (all four within eleven blocks) — correlated rotation is a hazard if a rotation-path bug ever ships; filed as **R12** (per-seat threshold jitter).
- Height 15,897 at 05:25 UTC · `fee.burned_micro = 900` · `history{disk_from 1, ram_window 512, indexed_txs 9}` · the treasury and the faucet unchanged overnight.
### Documentation and site (the sweep the launch deserved)
- New references: `docs/RPC.md` (all 20 methods, shapes, limits, live examples), `docs/FEES.md` (the fee rule, normatively), `docs/WALLET-GUIDE.md` (the deck as text), `docs/INCIDENTS.md` (the published incident index, eight entries), `networks/testnet-1/CHECKSUMS`, `ops/README.md`; `YELLOWPAPER.md` §19 addenda (permissionless accounts, the envelope fee and its commitment layout, the genesis fee field, allocations, chain identity, per-height certificate verification, receipts).
- **`docs/P3.2-IMPLEMENTATION-PLAN.md`** — what is left after the public testnet, from a full pass over the code, the fleet, the wallet, the site and every document: the critical path to the soak clock (**V1 validator-set changes** — seats are genesis-fixed today, so external operators run observers until it ships — → R11 verifier memory → four external voters), the H-batch hardening for public exposure (RPC timeouts/CORS/pagination/queue caps/WAL fsync/faucet), C2.8 retention, keys at rest, the wallet line, protocol items for mainnet, audit/assurance, three dated sprints with receipts.
- Every living document re-read against the running chain (165 stale items; the `validators/` kit rewritten for the live network — observers now, seats next; records banner-marked as records). The website: a new **/wallet** page (the guide with screenshots and the PDF), `/build` fee-aware quickstart + account step + the full RPC table, `/technology` cards for fees-as-genesis-fact / bounded memory / the ceremony, `/network` honest seat status + RAM guidance + a use-it band, `/receipts` entries for R9, the shielded wallet and the first rotation, explorer tiles for chain id · fees burned · signer fuse · history served, `facts.json` (27 claims) and `llms.txt` extended.
### Wallet v0.13.1 — `HashKinetics-Wallet.exe` sha256 `fb330c291c656e71a7d3473f9cffabb7a449a98ea7f8ddbcc09bd7062e16fb15` (built 2026-09-03, clean, 11 s incremental)
- Key files fsynced before rename (reserve-then-advance counters can no longer roll back on a crash) · a worker panic surfaces in ACTIVITY and clears the busy state instead of freezing the window · receipts awaited up to 60 s · an RPC failure is an error rather than "height 0" (which derived the epoch-0 stealth address) · the one-time spend-key budget shown in the SHIELDED panel · endpoints overridable via `HK_WALLET_RPC` / `HK_WALLET_FAUCET` · version in the footer · the shielded panel keeps its state and the page scrolls (from 2026-09-02). Release notes: `ops/RELEASE-NOTES-v0.13.1.md`.

## [0.13.0] — 2026-09-02 — 🌐 TESTNET-1 — fresh genesis with fees from block 1, bounded memory, the shielded wallet
The network relaunch release. staging-1 (`hashkinetics-1-557f2ea6`, 2026-08-27 → 2026-09-02, stopped at height 107,182) is retired and archived; **testnet-1** launches from a new ceremony genesis on this version.
### ⚠ NEW NETWORK — testnet-1 (join kit: `networks/testnet-1/`)
- Fresh validator keys for all four seats (stateful hash-based signing trees are never reused across chains), a new genesis digest / chain id, the same public endpoints (`rpc.` / `prover.` / `faucet.hashkinetics.org`, the explorer). Accounts and balances from staging-1 do not carry over — same seed, re-register through the faucet.
- **U4.b — the fee policy is a genesis fact.** `genesis.json` carries `chain.fee = {micro, from_height}`; on testnet-1 that is **100 micro per envelope from height 1**, burned. No activation height, no coordinated roll, and a node started with `HK_FEE_MICRO`/`HK_FEE_FROM` on such a network logs that the override is ignored — a validator cannot talk itself onto a fork. (Networks without the field keep the v0.12 behaviour: config decides.)
- **Genesis allocations.** `genesis-build --alloc <AUTH0>:<micro>` funds a self-custodied account whose id is derived from its nonce-0 auth commitment (squat-proof, exactly like `Tx::AccountCreate`); `--demo-accounts [$]` seeds the five public-seed demo accounts. The faucet treasury is funded at genesis; its seed never left the gateway — only the commitment entered the genesis. `hk-node account-info` prints the "genesis auth" line for this.
- `genesis-build` prints the digest and the chain id it implies; `docs/CEREMONY-TESTNET-1.md` has the procedure, the rules behind each step, and the record.
### R10 v2 — bounded memory, disk-served history, restore that cannot start at the wrong height
- The in-RAM decided map (every block + its hash-signature-heavy commit certificate, kept to serve value-sync) was **5.4 GB per node** on the 7.9 GB fleet at 104k heights, with a 13 KB state snapshot — measured 2026-09-02. Now only the newest **512** heights stay in RAM (`HK_DECIDED_WINDOW`, 0 = unbounded); a peer asking for anything older is served from the on-disk block log, re-checked against its certificate before it leaves the node.
- **The engine's start height comes from the chain** (snapshot + replay), never from the shape of the block log — the v0.12.0 R10 derived it from disk and a live validator restarted at height 0. A node advertises as servable only the gap-free suffix of block files that reaches its tip (a thin-teleported node says "from 49,929", not "from 1"), and the explorer's `hk_getBlocks` walks that suffix instead of listing 100k files per call.
- **Restore no longer rehydrates history into RAM.** Replay covers the few blocks past the snapshot; the v0.11.2 search indexes over older history are rebuilt by a background pass (decode → index → drop; `HK_INDEX_HISTORY=0` skips it on a voter that serves no explorer). A validator votes seconds after a restart instead of minutes; `hk_getAccountTxs` orders by height so the pass can land behind live commits. `hk_chainInfo.history` reports `disk_from`, `ram_window`, `indexed_txs`.
### The wallet — HashKinetics-Wallet.exe v0.13.0
- **Fee-aware (U4):** reads the chain's fee policy, shows it under the balance, `max` = balance − fee, and refuses locally what the chain would refuse (amount + fee > balance) so a ratchet index is never burned on a doomed envelope; the fee refusal receipt is explained in plain words. The header shows the chain id it is talking to.
- **Shielded (U3v2):** shield → pool, unshield → me, fully shielded payment to an `hkaddr:` stealth address with a memo, pool scan with spent status, one-time disclosure packages — the same library code the CLI wallet and the P2 demos proved, proofs made on the public prover on a worker thread (each STARK takes a while; the UI never blocks). `shield.json` next to `account.json` holds the shielded master and the reserve-then-advance counters — back it up too; restoring the shielded side from the account seed alone is deliberately not offered (a reused one-time leaf after a lossy restore would leak spend authority).
- The faucet's `/health` now reports the chain's fee alongside the drip size.
### Gate — chain/rehearsal.sh (devnet, 2026-09-02)
- Build clean · hk-state **14/14** (new: genesis-bound fee applies from block 1, field round-trips, legacy genesis parses) · hk-node **18/18** (new: gap-free suffix floor) · binary `071e86da…81fb1`.
- **Ceremony rehearsal:** 4 independent `keygen`s → `genesis-build --fee-micro 100 --fee-from 1 --alloc <treasury>:1,000,000,000 --demo-accounts 50` (vk pins fetched, digest + chain id printed and re-checked with `sha256sum`) → four validators from that genesis: height 5 in well under a minute, `chain_id = hashkinetics-1-<digest>`, `fee = {100, from 1}`, node log `fee policy is genesis-bound`, treasury 1,000,000,000 and demo org $50 funded at genesis. **The first transaction on the chain paid the fee:** treasury `AccountCreate` 250,000 → `burned_micro = 100`, treasury 999,749,900, user 250,000. A full-balance sweep refused `insufficient balance: have 249900, need 250000`; a partial send landed (user 149,900, burned 200).
- **R10 v2 restore shapes on a live validator (quorum held 3/4):** full log 28→43 · suffix-only log (files below tip−6 deleted; snapshot at 32) 35→59, advertised `disk_from = 33` — replay stopped at the snapshot and the synced gap was persisted, every file 33..59 present, `hk_getBlocks.earliest` agrees · wiped log with only the snapshot 51→75, restore line `tip=48 disk_files=0 servable_from=49`, engine `Consensus is ready start_height=49` (the block log said nothing; the chain height decided — the exact failure v0.12.0 had) · wiped log + `HK_FEE_FROM=999`: 70→89, override refused (`ignored — the fee policy is bound to this genesis`), no divergence. Each shape rejoined within ~15 s of RPC coming up.
- **Observer from genesis** against validators whose RAM window was 8: synced to the tip (92) with everything below tip−8 served from disk, zero serve refusals, zero divergence; `hk_chainInfo.history.ram_window = 8`.
- Harness note for the record: one check in the second run flagged itself — the "only block #1" scenario had no block 1 left because the preceding suffix-only scenario had deleted it, so it exercised the wiped-log shape twice; reordered in the script. RSS on the rehearsal box was 6.8 GB per validator with an 8-block window — that is the SP1 verifier client's fixed footprint, not history (see R11).
- **GATE GREEN (fourth run, same binary `071e86da…`, after the launch):** 34/34 — the only-block-#1 shape now reads `tip=32 disk_files=1` and rejoined 33→58; shapes A/C/D and the observer (93 heights) as above; rehearsal genesis `ae58f025…`. The third run had printed 33/34: the one FAIL was the harness reading `tip=` out of a log the node writes with ANSI colour even into files (field names are colour-wrapped, so `tip=` is never contiguous) — stripped before parsing; the same run "hung" after its verdict because the node launcher left a forked copy of the script holding the pipeline's stdout — nodes are now `exec`'d. Both fixes are in `chain/rehearsal.sh`; the node's own behaviour was correct in every run.
### Freshness sweep (post-launch, 2026-09-02 night)
- Every living document re-stamped against testnet-1 / v0.13.0 — several public docs were still snapshots from earlier releases (the architecture map from day one, the audit scope from v0.9.7, the devnet runbook from v0.10.0, key-management hardening status ending at R6, onboarding saying "minimum v0.11.0 to sync staging-1"). Now: architecture status through 0.13.0; audit scope explicitly includes the protocol fee and its genesis binding, genesis allocations, R9, R10 v2 (disk-served history with certificate re-check) and the wallet's shielded side; key-management status through R10 v2 and the fresh-trees-per-chain rule; onboarding minimum v0.13.0; README chain id; the site's wallet card and build badge; `facts.json` gained the ceremony and shielded-wallet claims. Historical entries (receipts timeline, changelog rows, dated measurements) are left as they were — they are records, not status.
### The launch (fleet, 2026-09-02)
- **Ceremony on the fleet (ops/ceremony-testnet-1.sh, Cloud Shell):** versioned binary relayed and sha-verified on all four hosts (`071e86da…81fb1`) → fresh `keygen` on every host into a new home (val-0 `F874765D…`, val-1 `42C00CD6…`, val-2 `E77F5F47…`, gateway `F7D75524…`) → four `validator.json` collected → `genesis-build` on the gateway with vk pins fetched from the fleet prover, `--fee-micro 100 --fee-from 1`, the faucet treasury's nonce-0 auth commitment (`--alloc`, id `6c0466c5…264ad`, 1,000,000,000,000 micro) and the demo accounts (org $50) → genesis distributed byte-identically (the VM service accounts turned out to be read-only on object storage, so the 12 KB file went host-to-host over ssh as base64) → **digest `4e4ea68d48cba1ad4cc7155c19e7768f1fa2cbc99ba0f2b47c58948ec9e971c7` on all four, chain id `hashkinetics-1-4e4ea68d`**, `chain_start_time` 1788362219.
- **staging-1's last breath:** val-0 h107,176 · val-1 h107,178 · val-2 h107,181 · gateway h107,182 (`hashkinetics-1-557f2ea6`), then all four stopped. Every host keeps its full staging-1 home (`hk-staging-1-final`: snapshot, block log, signer state); the gateway also holds a snapshot-first tar of the complete log (2,932,293,982 bytes), and the same bytes are in object storage since 18:21 UTC — the VM identity is read-only there, so the upload ran from the gateway with a one-hour operator token passed over ssh stdin.
- **Launch:** homes swapped, live binary ← v0.13.0, all four started within the same minute; the SP1 verifier client's init (~5 min on e2-standard-2) was the only wait; first blocks decided as the third validator came ready. Verified ~10 minutes later: all four on `hashkinetics-1-4e4ea68d`, heights 54–70 across the reads, 19–29 signed votes per node per 30 s, `fee = {100, from 1, burned 0}`, `history = {disk_from 1, ram_window 512}`, faucet `/health` reporting the treasury at 1,000,000,000,000 micro and the chain's fee, public RPC edge serving the new genesis digest.
- **Memory, honestly:** ~6.7 GB RSS per node right after launch with a ~70-block decided map — that is the SP1 verifier client's retained init footprint (R11), not history. R10 v2 keeps the map bounded from here on.

## [0.12.2] — 2026-09-02 — 💸 U4 + R9 (R10 removed) — the shippable fee release
The salvage of the aborted v0.12.0/0.12.1 roll: the two good pieces ship, the broken one is cut.
### ⚠ CONSENSUS-BEHAVIOR CHANGE — upgrade before the activation height
- **U4 — flat protocol fee.** From a coordinated activation height on staging-1 (sized at roll time against the VOTER tips, set via `HK_FEE_FROM`), every transaction envelope pays a flat **100 micro** in the test asset, charged to the sender and **burned**. Debited BEFORE the payload executes (a full-balance sweep honestly refuses) and **refunded in full when a transaction refuses** — a refusal never moves money. New refusal: `insufficient balance for the protocol fee`. The `fees_burned` counter enters the state commitment only once nonzero (upgraded and pre-upgrade nodes stay byte-identical until activation — the rolling upgrade cannot fork early). `hk_chainInfo` reports `fee.{micro,from_height,burned_micro}`; snapshots are versioned `snapshot2.bin` (the pre-fee `snapshot.bin` is read once on upgrade). Anti-spam floor + a mechanism rehearsal for testnet-1; refusals are free by design.
- **R9 — rotation guards can no longer wedge a validator.** After the val-0 wedge (a previously-issued rotation cert that never committed left the guards blocking forever, burning the tree to zero), the guards now hold only while an issue is FRESH; after 10 minutes with no on-chain effect — or after any restart — the same trigger fires again and the re-issue REPLACES the stale cert. Acceptors are epoch-monotone, so duplicates stay harmless; a gauge warning names the wedge instead of burning silently.
### Removed vs 0.12.0/0.12.1
- **R10 (bounded decided window + disk-served value-sync) is REMOVED.** It assumed a complete on-disk block log, but the staging nodes keep only block #1 on disk and value-sync the rest from peers — so R10's restore started the engine at height 0 (stuck). Restore/`start_height`/`decided`/value-sync are back to the proven v0.11 behavior. The memory/fast-restore win it aimed for returns later as its own release, redesigned to respect peer-sync nodes and tested against a sparse block log (not just a fresh devnet).
### Roll discipline (learned 2026-09-02)
- One voter at a time; confirm the node is actually **voting** (not merely `active`) before the next; gateway last; size the activation height against voter tips, never the public RPC edge.
### The fleet roll (2026-09-02) — DEPLOYED, and R9's production receipt
- Rolled val-1 → val-0 → val-2 → gateway, each verified voting before the next; the legacy `snapshot.bin` decode restored real v0.11 snapshots cleanly on every node (height-preferring loader picked the freshest image over a stale `snapshot2.bin` left by the aborted roll). **All four voters on v0.12.2, fee policy armed fleet-wide: `{micro: 100, from_height: 110000, burned: 0}`.**
- **R9 healed a validator with no human in the loop.** val-0 had burned its fresh epoch-4 tree to zero during the day (the same wedge as the morning incident, reproduced within ~10 hours on the un-fixed v0.11 binary). Rolled to v0.12.2, its first tick found the guard un-wedged, logged `R1.b tick: leaf budget under threshold … issuing rotation cert out-of-band`, pushed the epoch-5 cert to all three peers, and within a minute the node was signing again with zero refusals. Cadence recovered from a crawl to ~26 blocks/min as soon as its signer lived.

> **STATUS of the entries below:** [0.12.0] and [0.12.1] were the in-development work and its aborted first roll — kept for the record. They shipped R10, which v0.12.2 removes.

## [0.12.1] — 2026-09-02 — 🎯 activation height corrected (the gateway's height is not the chain's height)
- One-line release: `FEE_ACTIVATION_HEIGHT` 96,000 → **110,000**. The original constant was sized against the public RPC's height while the gateway was ~14k blocks behind the voters (post-restore catch-up — a lag class R10 itself eliminates going forward). The real chain had already passed 96,000 when the roll began; two upgraded voters were one tx-carrying block from a 2-2 state split. Caught before any transaction landed — the fleet was reverted to v0.11.0 within minutes (versioned on-disk binaries), no divergence occurred, and BFT safety guaranteed the worst case was a halt, never corruption. New operating law, now in the constant's doc comment: **activation heights are sized against voter tips, never an RPC edge.**

## [0.12.0] — 2026-09-02 — 💸 U4 + R9 + R10: fees are law, rotation can't wedge, memory is bounded
### ⚠ CONSENSUS-BEHAVIOR CHANGE — upgrade before height 110,000
- **U4 — flat protocol fee.** From activation height **110,000** on staging-1, every transaction envelope pays a flat **100 micro** in the test asset, charged to the sender and **burned**. The fee is debited BEFORE the payload executes (a full-balance sweep now honestly refuses: the tx's own spend sees the post-fee balance) and **refunded in full when a transaction refuses** — a refusal never moves money, exactly as before. New refusal: `insufficient balance for the protocol fee`. A `fees_burned` counter enters the state commitment only once nonzero, so upgraded and pre-upgrade nodes stay byte-identical until activation — a rolling fleet upgrade cannot fork early. `hk_chainInfo` now reports `fee.{micro,from_height,burned_micro}`. Honesty: this is an anti-spam floor and a mechanism rehearsal for testnet-1's real fee schedule — not a fee market; refusals are free by design (each attempt still burns the SENDER's one-time key material). U4.b (testnet-1): fee parameters move into genesis, like the vk pins.
- **R9 — rotation guards can no longer wedge a validator.** Production incident (2026-09-02): a validator burned its epoch tree to zero WITH the auto-rotation tick armed — a previously-issued cert that never committed left the "already issued / still pending" guards blocking forever. Fix: those guards now hold only while an issue is FRESH; after 10 minutes with no on-chain effect (or after any restart), the same trigger fires again and the re-issue REPLACES the stale cert. Acceptors are epoch-monotone, so duplicates stay harmless. A gauge warning now names the wedge instead of burning silently.
- **R10 — bounded memory, disk-served history.** The in-RAM decided map (full block history + hash-signature-heavy commit certificates, kept for value-sync serving) had become the fleet's ceiling: 5–7 GB per node at ~88k blocks, and a ~10-minute multi-GB restore. Now only the most recent **512 heights** stay in RAM (`HK_DECIDED_WINDOW`); anything older is served to value-sync straight from the disk block log via the same reconstruction the independent-observer receipt proved byte-identical. Restores no longer rehydrate history at all (it is decoded once for the search indexes and dropped): **gate measured RPC back in 1 second**. Steady-state memory becomes O(window), not O(height). Snapshots move to a versioned `snapshot2.bin` (the old `snapshot.bin` is read once, on upgrade, then superseded).
### The gate (devnet, 2026-09-02)
- hk-state 13/13 (new: fee burn/refund/activation/snapshot-roundtrip) · hk-node 17/17 (new: R9 re-arm) · live drill: burn counter 100→200 across exactly the two successful txs; sweep refused fee-aware (`have 249900, need 250000`); refused tx cost nothing · node restart with divergent fee config **refused to run on app_hash divergence** (the refuse-on-mismatch posture, now covering fee policy) · correct relaunch: RPC back in 1 s, search served from the rebuilt disk index, snapshot2 written.

## [0.11.2] — 2026-09-02 — 🔎 SEARCH EVERYTHING — the explorer answers, the wallet links
Non-consensus (RPC + UI only; wire untouched — only the public gateway needs this to serve search).
### Added
- **Node-local search indexes** (never in C(Σ) — anyone rebuilds them by replaying): txid → (block, index) and account → full history (as sender OR counterparty, with kind tags). Built on every commit AND on restart (replay path indexes via the shared apply path; rehydrated blocks are decoded and indexed explicitly), so search covers the whole chain after any restart. At current scale the indexes are a few thousand entries — in-memory by design; a dedicated indexer service is deliberately deferred to testnet-1 traffic levels.
- **Two RPCs:** `hk_getTx {txid}` → block height, in-block index, full summary (from the block log — answers forever, even after the receipt ring evicts), receipt if still cached; `hk_getAccountTxs {id, limit}` → newest-first history with kinds + total.
- **Explorer: search anything** — one box auto-detects block height / txid / account id / nullifier. Account view shows balance (with the honest "shielded holdings are invisible by design" note), ratchet index, and clickable history; tx view links to its block; every txid and sender in block details is clickable. **Deep links:** `#tx=…`, `#account=…`, `#block=…` — shareable, and the wallet uses them.
- **Wallet v0.11.2:** every drip and payment in the activity log now carries a **"view ↗" link** straight to its transaction in the explorer, and the account header gains "view on explorer ↗".

## [0.11.1] — 2026-09-02 — 🖥️ THE WALLET RELEASE — the front door behind a double-click
Non-consensus addition (new crate only; no protocol or wire changes — v0.11.0 nodes are unaffected).
### Added
- **HashKinetics Wallet for Windows** (`chain/crates/hk-wallet-gui` → `HashKinetics-Wallet.exe`): create or restore a post-quantum keychain, tap the faucet (auto-detects create-and-fund vs top-up), watch your balance, and pay any account id — against the live staging network, no CLI required. Native Rust (egui) — no browser runtime, no installer; one `cargo build --release -p hk-wallet-gui` produces the .exe.
- Same custody discipline as everything else in this repo: keys are generated locally and never leave the machine (the faucet only sees a commitment); the L-ratchet nonce is persisted BEFORE submit and rolled back only on a definitive on-chain refusal; the chain's nonce is adopted when a restored backup drifts. The wallet file is byte-compatible with the CLI's `account.json` — CLI and GUI share a wallet.
- v1 is the transparent journey (join → get funded → pay). Shielded operations via the public prover are the next wallet milestone.
### The gate (live staging chain, 2026-09-02)
- Wallet created → the second-ever public faucet drip → balance 0.100000 → a payment **signed at ratchet index 0**, receipt `ok`, publicly visible in the explorer (block 86,063, round 0, hash-based commit certificate).
### Honesty
- The distributed .exe is an **unsigned testnet build** — Windows SmartScreen will warn on first run. Verify the published sha256 instead of trusting the popup; code signing arrives with the production testnet.

## [0.11.0] — 2026-09-02 — 🚪 U1+U2: THE FRONT DOOR — anyone can join, get funded, and pay
The usage sprint opens. Until today, accounts were genesis-only: the chain was something you could *verify* but not *join*. v0.11.0 adds runtime account creation, a public faucet, and a self-custody CLI — a stranger goes from nothing to paying on-chain in under a minute.
### ⚠ CONSENSUS-BREAKING — upgrade before use
- A new transaction variant is appended to the wire format. Nodes **below v0.11.0 cannot decode any block containing an `AccountCreate`** and will refuse the batch. All validators and syncing nodes must upgrade BEFORE the first account creation commits; the staging-1 join kit now pins v0.11.0 as the minimum.
### Added
- **`Tx::AccountCreate { id, auth_commit, asset, amount }`** — permissionless, squat-proof runtime accounts: consensus enforces `id = H(DOM_ACCOUNT_ID ‖ auth_commit)`, so an identity can only be claimed by whoever holds the key material behind it, and no existing account can ever be overwritten. The sender funds the opening balance from its own (0 allowed); structural refusals run before money moves, so a failed create never debits. New refusals: `account already exists`, `account id does not match H(auth_commit)`. Explorer shows creations as `account_create`.
- **Self-custody account CLI** — `account-new` (32 bytes of OS entropy → keychain + derived id, refuses to overwrite key material), `account-info`, `account-balance`, `account-send`, `account-create` (sponsor anyone onto the chain from your own funds). The L-ratchet nonce follows the consensus signer's reserve-then-sign discipline: persisted BEFORE submit, rolled back (and persisted) only on a definitive on-chain refusal, and re-synced from the chain when the local file drifts — a crash can never re-sign a spent index.
- **`faucet-serve` — the public faucet** (`POST /drip {auth_commit}` creates+funds; `{account}` tops up; `GET /health`). Per-IP cooldown + global daily cap (X-Forwarded-For aware, failed drips refund the stamp); CORS-by-construction (every response carries the header, and the website calls it with no custom headers — a simple request needs no preflight). Until fees land, rate limits are the anti-spam floor.
- **hashkinetics.org/faucet** — three steps on one page: generate a keychain locally, paste the auth commit, spend. Keys never leave the user's machine; the faucet only ever sees a commitment.
### The gate (devnet, 2026-09-02 — the first complete user journey)
- `account-new` → id `00c6b30e…` · POST /drip → **AccountCreate committed** (txid `0d7a5f93…`) · balance $0.25 · the newcomer's OWN first signature (ratchet index 0) paid $0.05 back — receipt `ok: 1 event(s)` · balance $0.20. Squat, duplicate, and overdraft refusals covered by `u1_runtime_account_creation_faucet_flow` (hk-state 12/12, hk-node 16/16).
### Honesty
- Staging drips are deliberately tiny — the staging genesis carries a ~$50 total transparent supply. The production testnet (testnet-1) launches with a real faucet treasury (private seed) baked into genesis.

## [0.10.10] — 2026-09-01 — 🛡️ R8 + R1.b: a validator can nap without eating its keychain
Built for the external-validator era: strangers' nodes WILL go offline, fall behind, and wedge — ours only survived because we babysat them. Two shipped fixes make a parked validator boring instead of fatal.
### The gate (devnet, 2026-09-01 — the val-0 scenario, reproduced and cured)
- A voter was wedged below tip with value-sync disabled: it spent **2 leaves** (one round, before peer evidence arrived), then went silent — claim path confirmed live (`532` future-height votes recorded from all 3 distinct peers; core-consensus buffers them at exactly the instrumented ingress).
- Sync restored: it climbed **850 heights in ~70 s** while R8 suppressed every futile vote attempt en route (9× `R8: behind the network — abstaining … height=12 peer_tip=Some(830)`), landed **byte-identical** with the fleet (`app_hash c59d0effa184cb68` at h=862, 5 ms apart), and cast a live Prevote at **h=863** — the first possible height.
- The leaf ledger: the wedged voter finished at **69 used vs 2,358–2,566** on its peers — ~39 spent total (stuck round + live voting after rejoin), versus ~1,700 that pre-R8 catch-up voting would have burned. Forensics closed en route: rebroadcast re-sends stored votes (never re-signs, free), a truly-stuck driver is protocol-quiescent (~2 leaves per event), and the historical 32,768-leaf burn is now fully attributed to catch-up voting — the exact path R8 gates.
### Fixed
- **R8 — abstain-while-behind: futile votes no longer burn one-time leaves.** A validator wedged below the network tip used to cycle consensus rounds at its stale height — every nil prevote/precommit spending an irreplaceable LMS leaf (a fresh 32,768-leaf tree burned to ZERO in ~20 h of exactly this). New `Context::abstain_threshold()` (default `None` = upstream unchanged — the same pattern as `validator_set_at` and `placeholder_signature`): when corroborated peer evidence puts the network ≥ gap heights ahead, the driver skips all three leaf-spending arms (vote signing, proposal signing, value building). Abstaining is indistinguishable from being offline, which BFT already tolerates. The signal: future-height votes/proposals (already buffered by core-consensus) record per-validator height claims — **only current-set members count** (spam-proof), and the trigger is the **second-highest claim among distinct validators**, so one faulty validator can't fire it and a lone ahead-of-quorum node can't stall the majority. Default 20-height gap (`HK_ABSTAIN_GAP` overrides, `=0` disables). Ledgered residual: a same-height stall (quorum-loss halt) still spends leaves on live rounds — those rounds could actually commit, and the burn decays with round-timeout backoff.
- **R1.b — rotation threshold now evaluated on a tick, not only on commit.** The leaf-budget check lived in the commit path — a node that never commits (parked, minority partition: exactly the node whose leaves are draining) NEVER evaluated it, and burned to zero with the threshold armed. A 60 s tick (observers no-op) reads the live gauge, runs the SAME `rotation_due` (threshold clause only — unit-tested shape), and on due issues the root-signed cert **out-of-band**: self-enqueued into the foreign-cert queue AND pushed to every gossip peer via `hk_submitRotation` — the peer-carried revival path, self-served before exhaustion instead of human-driven after it. Epoch-monotone acceptors make duplicate issuance harmless by construction.
### Together
R8 preserves the leaves; R1.b spends the preserved leaves on a rotation cert while they still exist. The incident pattern (parked → burned → dead → human revival) becomes: parked → silent → self-rotated → rejoins. Binary: sha256 `2d6562912b90e6a319c126bf8b9c66bb97f03fd34f7f8b1b0e93c25c1adfe8a6`, 68,675,328 bytes.

## [0.10.9] — 2026-08-31 — 🍃 R5.4: SYNC SPENDS NO LEAVES — the join kit stops paying rent
### Found in production, same day (deploy receipts)
- **R3 CLOSED by measurement:** val-2 restarted on v0.10.9 and was back at tip, in lockstep with the gateway, in **under 4 minutes** (was: ~25 min rehydrate then trailing forever). R5.2's parallel apply cured deep-restart trailing as a side effect. Corollary observed: with 3/4 voters the chain slows to ~4 blk/min (zero timing margin) and self-heals at 4/4 — restarts stay one-at-a-time.
- **Join-kit gap: a node without `HK_PROVER_URL` wedges at the first proof-carrying shielded tx.** The SP1 verifier wires only when the env points at an hk-prove endpoint (vks fetched once, checked against genesis `vk_pins`); without it the chain keeps the RejectAll default — correct refusal posture for TRAFFIC, but a SYNCING node then rejects a canonical block's shield tx and diverges (`app_hash divergence`, observed at height 2,389 on the observer: gateway `ok: 1 event(s)` vs observer `rejected: pool proof rejected`). Fixed operationally (fleet nodes point at an internal vk-cache; public joiners use `https://prover.hashkinetics.org`) and documented in the join kit + onboarding. Real fix queued: ship the verifying keys IN the join kit so verification needs no live prover.
- **Leaf-freeze verified:** with R5.4 live the observer's signer `remaining` froze while height advanced — sync now spends zero leaves; the unwedge watchdog is dormant insurance.
### Fixed
- **Syncing no longer consumes one-time signature leaves.** Upstream malachite fabricates an internal `SignedProposal` for every synced value and signs it with the node's REAL key ("for now we keep all happy…", its own TODO admits) — harmless for Ed25519, but for stateful LMS/HSS that's one irreplaceable leaf per synced block: the first external observer spent its entire 32,768-leaf tree syncing and wedged. New `Context::placeholder_signature()` (default `None` = upstream behavior, same shape as R6's `validator_set_at`): `HkContext` returns an empty dummy for these internal-only, never-verified proposals — zero leaves spent, sync no longer depends on the local signer at all. The v0.10.8 unwedge watchdog becomes dormant insurance.

## [0.10.8] — 2026-08-31 — ⚡ R5.2: PARALLEL VERIFICATION — the join kit learns to outrun the chain
The observer-from-genesis receipt exposed the truth: catch-up collapsed to ~2 blocks/min against a chain adding ~27/min — a joiner that can never converge. Recon found the floor was never one thing:
### Fixed
- **Duplicate certificate verification eliminated.** Every SYNCED block verified its commit certificate twice — once on the sync path, again on the decide path, same certificate. `State.last_verified_certificate` (height, round, value-id) memoizes the sync-path pass; `decide` skips the re-verify on match. Live decides never set the memo and verify exactly as before. Half the hash-based signature work per synced block, ~15 lines.
- **One state-commitment walk per block, not two.** The post-apply `state_commitment()` (log line) and the next block's parent-hash check recomputed the SAME value — each an O(entire state, every nullifier ever) walk that GROWS with height (this, not constant-cost crypto, is why the observer's rate decayed). The post-apply hash is now cached and consumed by the next parent check; any error path drops the cache and recomputes honestly.
### Added
- **Batch signature verification hook** (`SigningProvider::verify_signed_votes_batch`, default `None` = serial fallback — same shape as R6's `validator_set_at`). The blanket `verify_commit_certificate` does one cheap ordered pass (duplicates, set membership, precommit reconstruction), then hands ALL LMS/HSS verifies to the provider's fast path. `HkSigningProvider` runs them on a dedicated **32 MiB-stack rayon pool** (`hk-consensus::par` — hbs-lms overflows default 2 MiB workers; capped at 8 threads). Boxed providers delegate, so the engine's `Box<dyn SigningProvider>` keeps the override.
- **Parallel envelope pre-verification.** A fat load-test-era block paid thousands of Lamport verifies serially INSIDE the state lock. The pure part (signing digest + Lamport over the tx's own fields) now runs `par_bools` outside the lock; `apply_block_verified` consumes the verdicts with check order IDENTICAL to the inline path (account → nonce → auth-commit → signature), so the success set is byte-for-byte unchanged whichever path a node takes. The key→account binding stays ordered.
- **Apply-cost telemetry** — `R5.2 apply-cost window` log every 100 blocks: pre/agg/apply/hash/save/snap ms. The 450 ms/block floor was an inference; now every node measures its own.
- **`HK_SNAPSHOT_EVERY`** env (default 16): the full-image snapshot + fsync grows with state and taxes catch-up; a grinding observer can widen the cadence (crash cost = replaying a few more blocks).
### Deliberately unchanged
- Engine-level batch pre-verify of whole sync batches (20-way across certificates) parked as **R5.2b** — measure first; the per-block wins may already clear the ~27 blk/min convergence bar.
### Proven in production, same day
- **Observer hot-swapped to v0.10.8** (e2-standard-8): sustained **71 blocks/min** measured over 2 minutes (was ~2 blk/min — 35×), net +44 blk/min against the live chain. Telemetry window at the early era: `pre=0 agg=0 apply=0 hash=0 save_ms≈3.2/blk` — after R5.2 the measured bottleneck is the per-block fsync, not crypto.
- **New failure mode found and fixed operationally: observer signer exhaustion.** A syncing non-validator SELF-SIGNS each synced proposal to drive the state machine (~1 LMS leaf/block; wedge-loops burn far more) — the original observer silently spent its ENTIRE 32,768-leaf tree (state file read `used = 0x8000` exactly), and every later sync value died as `Unexpected resume: Continue, expected Resume::SignedProposal`. Non-validators have no on-chain revival (their root isn't in the set), but their self-signatures are consumed only locally — so the remedy is: stop, rotate the spent `consensus_state.bin` aside, start (fresh tree). An **unwedge watchdog** (systemd timer, 2-min cadence) now does this automatically; the tree covers ~30k blocks per cycle. Proper fix parked as **R5.4: a syncing non-validator should not spend hash-based leaves at all** (accept sync values without self-signing, and let a failed SignProposal degrade gracefully instead of erroring the message).

## [0.10.7] — 2026-08-30 — 🧠 R6: PER-HEIGHT VALIDATOR-SET VERIFICATION — the chain remembers who signed what
### Added
- **Commit certificates now verify against the validator set AS OF their height** (the set that actually signed them), not the current set. `Context::validator_set_at(height)` (default `None` = old behavior) + the engine's `VerifyCommitCertificate` handler swaps in the historical set keyed by `certificate.height` — one interception point fixes both the sync and decide paths, no wire changes. `HkContext` answers from a shared **set history** (`(effective_from_height, set)`), seeded at genesis, reseeded at snapshot restore, appended on every committed rotation (live and replay).
- **`decide.rs` de-panicked:** a certificate failing verification used to `assert!` → kill the consensus actor → poison the shared validator mutex → half-dead node with a live RPC (the exact production corpse pose, twice). It now returns `InvalidCommitCertificate` with an error log; all `validators.lock()` sites recover poisoned guards.
### Proven in production, same day
- **val-0's THIRD revival:** E3 rotation cert issued offline + carried by a peer (`hk_submitRotation`), store transplant, then **organic sync across live rotation boundaries (E44/E7/E8) → adopted the fresh E3 tree mid-sync → voting. True 4/4 at 50,443+.**
- **Observer-from-genesis receipt (in progress):** a brand-new VM joined via `networks/staging-1` (public seed DNS, published fingerprint verified: `557f2ea6c…`), reports `chain_id: hashkinetics-1-557f2ea6`, and is re-verifying the entire chain from block 1 — every rotation boundary, every STARK.
### Operational findings (ledgered)
- Transplant tars must pack `snapshot.bin` FIRST (blocks-then-snapshot ordering left the snapshot ahead of the blocks tail → engine/chain height skew → wedge).
- Deep catch-up apply on e2-standard-2 cannot outrun the live chain (R5.2/R3 territory); interim cure = **thin teleport** (snapshot + newest ~400 blocks → 2-minute restore, lands at tip). R3 (resume-at-tip) is the real fix.

## [0.10.6] — 2026-08-30 — 🪪 THE IDENTITY RELEASE — chain_id bound to genesis; islands refused
### Added
- **`chain_id` is derived from the genesis digest** (`hashkinetics-1-557f2ea6` for staging-1) instead of a hard-coded label — a node on a different genesis visibly reports a different chain.
- **`hk_chainInfo` returns `genesis_digest`** — the full SHA-256 fingerprint, machine-checkable against `sha256sum genesis.json` and the public RPC.
- **Nodes refuse to peer across genesis:** each node advertises its digest in the libp2p identify handshake (`agent_version`); consensus-protocol peers with a mismatched digest are disconnected. Pre-gate peers are graced, so rolling upgrades cannot partition. `protocol_version` untouched.
- **R5 published:** continuation-driven value-sync (re-request at every capacity-freeing exit + stale-pending expiry; batch 5→20) — ~an order of magnitude on deep backlogs.
### Fixed
- Published staging-1 genesis fingerprint had a 3-nibble hand-transcription typo; now matches `sha256sum genesis.json` exactly.

## [0.10.5] — 2026-08-28 — 🔁 R-SERIES: SURVIVABLE KEY ROTATION — keys that retire themselves
### Added
- **R1 — threshold rotation:** a validator whose HSS leaf budget crosses <20% (6,553 of 32,768) automatically issues a root-signed rotation cert that rides its next proposal. First unattended production rotations observed overnight (epoch badges climbing on the public explorer).
- **R2 — survivable exhaustion + revival tooling:** an exhausted signer returns `Err` (engine swallows; node keeps syncing/serving) instead of halting; `hk-node issue-rotation` CLI + `hk_submitRotation` RPC let ANY peer carry a dead validator's revival cert (used in production for every revival since).
- **R4 — leaf gauge:** `hk_chainInfo.signer {epoch, remaining, capacity}` + tiered budget logs — operators watch the fuse.
- **WS-F closed:** `adopt_epoch_signer` — post-restore, the live signer rebuilds at the chain's epoch for our entry; per-epoch state files resume their own used-leaf counters (leaf reuse impossible across restarts).
- `networks/staging-1/` join kit (pinned genesis + PEERS + README) · v0.10.5 released immutable on GitHub.
### The incident that earned it (2026-08-27, staging)
- A validator burned all 32,768 one-time signatures and the chain **halted rather than reuse a leaf** — hash-based consensus keeping its core promise the hard way. Recovery was the designed path (offline root-signed cert carried by a peer), not a patch. Full postmortem in the honesty ledger.

## [0.10.4] — 2026-08-27 — ⚡ C2: THE THROUGHPUT LIFTS + GCP STAGING TESTNET LIVE
### Added
- **C2.1–C2.5:** admission pre-checks on submit · indexed mempool prune · tx gossip between mempools · block cap 256→1024 — **storm re-measure: 274 tx/s sustained** (2.2× the 0.10.3 baseline) · full test suite + demo suite green.
- **Public staging testnet on GCP:** 4× e2-standard-2 (3 validators + gateway), nginx-fronted public RPC (`rpc.hashkinetics.org`), explorer wired to it, seed DNS (`seed.hashkinetics.org`), 2s cadence.
- **C1p2.a measured (RTX 5090):** aggregation curve N=4…256 — `T_agg(N) = 2.66 s + 0.2882 s·N`, proof size CONSTANT (1,242 KB); 256 shielded spends → ONE proof in 75.4 s.

---

*Public log begins at the staging-testnet era (v0.10.4). Earlier internal build history is not part of the open-source release.*
