# HashKinetics — Changelog

All notable changes to this project. Versions are project-level (see `version.md` for per-crate status). Dates are IST-day of work.

## [0.13.1] — 2026-09-03 — 🔁 the first rotation on testnet-1 · the sweep after launch · wallet v0.13.1 (wallet-only release; the node stays v0.13.0)
### Receipts (testnet-1, 2026-09-03)
- **All four seats rotated their signing trees unattended at 04:03 UTC** — rotation certificates at heights **11,584 · 11,588 · 11,594 · 11,595** (one per validator; block times 1788373803–1788373814), every seat now `epoch 1`, zero blocks missed, cadence ~19 blocks/min since launch. Finding: fresh trees started together cross the 20% threshold together (all four within eleven blocks) — correlated rotation is a hazard if a rotation-path bug ever ships; filed as **R12** (per-seat threshold jitter).
- Height 15,897 at 05:25 UTC · `fee.burned_micro = 900` · `history{disk_from 1, ram_window 512, indexed_txs 9}` · the treasury and the faucet unchanged overnight.
### Documentation and site (the sweep the launch deserved)
- New references: `docs/RPC.md` (all 20 methods, shapes, limits, live examples), `docs/FEES.md` (the fee rule, normatively), `docs/WALLET-GUIDE.md` (the deck as text), `docs/INCIDENTS.md` (the published incident index, eight entries), `networks/testnet-1/CHECKSUMS`, `ops/README.md`; `YELLOWPAPER.md` §19 addenda (permissionless accounts, the envelope fee and its commitment layout, the genesis fee field, allocations, chain identity, per-height certificate verification, receipts).
- **`docs/P3.2-IMPLEMENTATION-PLAN.md`** — what is left after the public testnet, from a full pass over the code, the fleet, the wallet, the site and every document: the critical path to the soak clock (**V1 validator-set changes** — seats are genesis-fixed today, so external operators run observers until it ships — → R11 verifier memory → four external voters), the H-batch hardening for public exposure (RPC timeouts/CORS/pagination/queue caps/WAL fsync/faucet), C2.8 retention, keys at rest, the wallet line, protocol items for mainnet, audit/assurance, three dated sprints with receipts.
- Every living document re-read against the running chain (165 stale items; the `validators/` kit rewritten for the live network — observers now, seats next; records banner-marked as records). The website: a new **/wallet** page (the guide with screenshots and the PDF), `/build` fee-aware quickstart + account step + the full RPC table, `/technology` cards for fees-as-genesis-fact / bounded memory / the ceremony, `/network` honest seat status + RAM guidance + a use-it band, `/receipts` entries for R9, the shielded wallet and the first rotation, explorer tiles for chain id · fees burned · signer fuse · history served, `facts.json` (27 claims) and `llms.txt` extended.
### Wallet v0.13.1 — `HashKinetics-Wallet.exe` sha256 `fb330c291c656e71a7d3473f9cffabb7a449a98ea7f8ddbcc09bd7062e16fb15` (built 2026-09-03, clean, 11 s incremental)
- Key files fsynced before rename (reserve-then-advance counters can no longer roll back on a crash) · a worker panic surfaces in ACTIVITY and clears the busy state instead of freezing the window · receipts awaited up to 60 s · an RPC failure is an error rather than "height 0" (which derived the epoch-0 stealth address) · the one-time spend-key budget shown in the SHIELDED panel · endpoints overridable via `HK_WALLET_RPC` / `HK_WALLET_FAUCET` · version in the footer · the shielded panel keeps its state and the page scrolls (from 2026-09-02). Release notes: `ops/RELEASE-NOTES-v0.13.1.md`.

## [0.13.0] — 2026-09-02 — 🌐 TESTNET-1 — fresh genesis with fees from block 1, bounded memory, the shielded wallet
The network relaunch release. staging-1 (`hashkinetics-1-557f2ea6`, 2026-08-27 → 2026-09-02, stopped at height 107,182) is retired and archived; **testnet-1** launches from a new ceremony genesis on this version.
### ⚠ NEW NETWORK — testnet-1 (join kit: `networks/testnet-1/`)
- Fresh validator keys for all four seats (stateful hash-based signing trees are never reused across chains), a new genesis digest / chain id, the same public endpoints (`rpc.` / `prover.` / `faucet.hashkinetics.org`, the explorer). Accounts and balances from staging-1 do not carry over — same seed, re-register through the faucet.
- **U4.b — the fee policy is a genesis fact.** `genesis.json` carries `chain.fee = {micro, from_height}`; on testnet-1 that is **100 micro per envelope from height 1**, burned. No activation height, no coordinated roll, and a node started with `HK_FEE_MICRO`/`HK_FEE_FROM` on such a network logs that the override is ignored — a validator cannot talk itself onto a fork. (Networks without the field keep the v0.12 behaviour: config decides.)
- **Genesis allocations.** `genesis-build --alloc <AUTH0>:<micro>` funds a self-custodied account whose id is derived from its nonce-0 auth commitment (squat-proof, exactly like `Tx::AccountCreate`); `--demo-accounts [$]` seeds the five public-seed demo accounts. The faucet treasury is funded at genesis; its seed never left the gateway — only the commitment entered the genesis. `hk-node account-info` prints the "genesis auth" line for this.
- `genesis-build` prints the digest and the chain id it implies; `docs/CEREMONY-TESTNET-1.md` has the procedure, the rules behind each step, and the record.
### R10 v2 — bounded memory, disk-served history, restore that cannot start at the wrong height
- The in-RAM decided map (every block + its hash-signature-heavy commit certificate, kept to serve value-sync) was **5.4 GB per node** on the 7.9 GB fleet at 104k heights, with a 13 KB state snapshot — measured 2026-09-02. Now only the newest **512** heights stay in RAM (`HK_DECIDED_WINDOW`, 0 = unbounded); a peer asking for anything older is served from the on-disk block log, re-checked against its certificate before it leaves the node.
- **The engine's start height comes from the chain** (snapshot + replay), never from the shape of the block log — the v0.12.0 R10 derived it from disk and a live validator restarted at height 0. A node advertises as servable only the gap-free suffix of block files that reaches its tip (a thin-teleported node says "from 49,929", not "from 1"), and the explorer's `hk_getBlocks` walks that suffix instead of listing 100k files per call.
- **Restore no longer rehydrates history into RAM.** Replay covers the few blocks past the snapshot; the v0.11.2 search indexes over older history are rebuilt by a background pass (decode → index → drop; `HK_INDEX_HISTORY=0` skips it on a voter that serves no explorer). A validator votes seconds after a restart instead of minutes; `hk_getAccountTxs` orders by height so the pass can land behind live commits. `hk_chainInfo.history` reports `disk_from`, `ram_window`, `indexed_txs`.
### The wallet — HashKinetics-Wallet.exe v0.13.0
- **Fee-aware (U4):** reads the chain's fee policy, shows it under the balance, `max` = balance − fee, and refuses locally what the chain would refuse (amount + fee > balance) so a ratchet index is never burned on a doomed envelope; the fee refusal receipt is explained in plain words. The header shows the chain id it is talking to.
- **Shielded (U3v2):** shield → pool, unshield → me, fully shielded payment to an `hkaddr:` stealth address with a memo, pool scan with spent status, one-time disclosure packages — the same library code the CLI wallet and the P2 demos proved, proofs made on the public prover on a worker thread (each STARK takes a while; the UI never blocks). `shield.json` next to `account.json` holds the shielded master and the reserve-then-advance counters — back it up too; restoring the shielded side from the account seed alone is deliberately not offered (a reused one-time leaf after a lossy restore would leak spend authority).
- The faucet's `/health` now reports the chain's fee alongside the drip size.
### Gate — chain/rehearsal.sh (devnet, 2026-09-02)
- Build clean · hk-state **14/14** (new: genesis-bound fee applies from block 1, field round-trips, legacy genesis parses) · hk-node **18/18** (new: gap-free suffix floor) · binary `071e86da…81fb1`.
- **Ceremony rehearsal:** 4 independent `keygen`s → `genesis-build --fee-micro 100 --fee-from 1 --alloc <treasury>:1,000,000,000 --demo-accounts 50` (vk pins fetched, digest + chain id printed and re-checked with `sha256sum`) → four validators from that genesis: height 5 in well under a minute, `chain_id = hashkinetics-1-<digest>`, `fee = {100, from 1}`, node log `fee policy is genesis-bound`, treasury 1,000,000,000 and demo org $50 funded at genesis. **The first transaction on the chain paid the fee:** treasury `AccountCreate` 250,000 → `burned_micro = 100`, treasury 999,749,900, user 250,000. A full-balance sweep refused `insufficient balance: have 249900, need 250000`; a partial send landed (user 149,900, burned 200).
- **R10 v2 restore shapes on a live validator (quorum held 3/4):** full log 28→43 · suffix-only log (files below tip−6 deleted; snapshot at 32) 35→59, advertised `disk_from = 33` — replay stopped at the snapshot and the synced gap was persisted, every file 33..59 present, `hk_getBlocks.earliest` agrees · wiped log with only the snapshot 51→75, restore line `tip=48 disk_files=0 servable_from=49`, engine `Consensus is ready start_height=49` (the block log said nothing; the chain height decided — the exact failure v0.12.0 had) · wiped log + `HK_FEE_FROM=999`: 70→89, override refused (`ignored — the fee policy is bound to this genesis`), no divergence. Each shape rejoined within ~15 s of RPC coming up.
- **Observer from genesis** against validators whose RAM window was 8: synced to the tip (92) with everything below tip−8 served from disk, zero serve refusals, zero divergence; `hk_chainInfo.history.ram_window = 8`.
- Harness note for the record: one check in the second run flagged itself — the "only block #1" scenario had no block 1 left because the preceding suffix-only scenario had deleted it, so it exercised the wiped-log shape twice; reordered in the script. RSS on the rehearsal box was 6.8 GB per validator with an 8-block window — that is the SP1 verifier client's fixed footprint, not history (see R11).
- **GATE GREEN (fourth run, same binary `071e86da…`, after the launch):** 34/34 — the only-block-#1 shape now reads `tip=32 disk_files=1` and rejoined 33→58; shapes A/C/D and the observer (93 heights) as above; rehearsal genesis `ae58f025…`. The third run had printed 33/34: the one FAIL was the harness reading `tip=` out of a log the node writes with ANSI colour even into files (field names are colour-wrapped, so `tip=` is never contiguous) — stripped before parsing; the same run "hung" after its verdict because the node launcher left a forked copy of the script holding the pipeline's stdout — nodes are now `exec`'d. Both fixes are in `chain/rehearsal.sh`; the node's own behaviour was correct in every run.
### Freshness sweep (post-launch, 2026-09-02 night)
- Every living document re-stamped against testnet-1 / v0.13.0 — several public docs were still snapshots from earlier releases (the architecture map from day one, the audit scope from v0.9.7, the devnet runbook from v0.10.0, key-management hardening status ending at R6, onboarding saying "minimum v0.11.0 to sync staging-1"). Now: architecture status through 0.13.0; audit scope explicitly includes the protocol fee and its genesis binding, genesis allocations, R9, R10 v2 (disk-served history with certificate re-check) and the wallet's shielded side; key-management status through R10 v2 and the fresh-trees-per-chain rule; onboarding minimum v0.13.0; README chain id; the site's wallet card and build badge; `facts.json` gained the ceremony and shielded-wallet claims. Historical entries (receipts timeline, changelog rows, dated measurements) are left as they were — they are records, not status.
### The launch (fleet, 2026-09-02)
- **Ceremony on the fleet (ops/ceremony-testnet-1.sh, Cloud Shell):** versioned binary relayed and sha-verified on all four hosts (`071e86da…81fb1`) → fresh `keygen` on every host into a new home (val-0 `F874765D…`, val-1 `42C00CD6…`, val-2 `E77F5F47…`, gateway `F7D75524…`) → four `validator.json` collected → `genesis-build` on the gateway with vk pins fetched from the fleet prover, `--fee-micro 100 --fee-from 1`, the faucet treasury's nonce-0 auth commitment (`--alloc`, id `6c0466c5…264ad`, 1,000,000,000,000 micro) and the demo accounts (org $50) → genesis distributed byte-identically (the VM service accounts turned out to be read-only on object storage, so the 12 KB file went host-to-host over ssh as base64) → **digest `4e4ea68d48cba1ad4cc7155c19e7768f1fa2cbc99ba0f2b47c58948ec9e971c7` on all four, chain id `hashkinetics-1-4e4ea68d`**, `chain_start_time` 1788362219.
- **staging-1's last breath:** val-0 h107,176 · val-1 h107,178 · val-2 h107,181 · gateway h107,182 (`hashkinetics-1-557f2ea6`), then all four stopped. Every host keeps its full staging-1 home (`hk-staging-1-final`: snapshot, block log, signer state); the gateway also holds a snapshot-first tar of the complete log (2,932,293,982 bytes), and the same bytes are in object storage since 18:21 UTC — the VM identity is read-only there, so the upload ran from the gateway with a one-hour operator token passed over ssh stdin.
- **Launch:** homes swapped, live binary ← v0.13.0, all four started within the same minute; the SP1 verifier client's init (~5 min on e2-standard-2) was the only wait; first blocks decided as the third validator came ready. Verified ~10 minutes later: all four on `hashkinetics-1-4e4ea68d`, heights 54–70 across the reads, 19–29 signed votes per node per 30 s, `fee = {100, from 1, burned 0}`, `history = {disk_from 1, ram_window 512}`, faucet `/health` reporting the treasury at 1,000,000,000,000 micro and the chain's fee, public RPC edge serving the new genesis digest.
- **Memory, honestly:** ~6.7 GB RSS per node right after launch with a ~70-block decided map — that is the SP1 verifier client's retained init footprint (R11), not history. R10 v2 keeps the map bounded from here on.

## [0.12.2] — 2026-09-02 — 💸 U4 + R9 (R10 removed) — the shippable fee release
The salvage of the aborted v0.12.0/0.12.1 roll: the two good pieces ship, the broken one is cut.
### ⚠ CONSENSUS-BEHAVIOR CHANGE — upgrade before the activation height
- **U4 — flat protocol fee.** From a coordinated activation height on staging-1 (sized at roll time against the VOTER tips, set via `HK_FEE_FROM`), every transaction envelope pays a flat **100 micro** in the test asset, charged to the sender and **burned**. Debited BEFORE the payload executes (a full-balance sweep honestly refuses) and **refunded in full when a transaction refuses** — a refusal never moves money. New refusal: `insufficient balance for the protocol fee`. The `fees_burned` counter enters the state commitment only once nonzero (upgraded and pre-upgrade nodes stay byte-identical until activation — the rolling upgrade cannot fork early). `hk_chainInfo` reports `fee.{micro,from_height,burned_micro}`; snapshots are versioned `snapshot2.bin` (the pre-fee `snapshot.bin` is read once on upgrade). Anti-spam floor + a mechanism rehearsal for testnet-1; refusals are free by design.
- **R9 — rotation guards can no longer wedge a validator.** After the val-0 wedge (a previously-issued rotation cert that never committed left the guards blocking forever, burning the tree to zero), the guards now hold only while an issue is FRESH; after 10 minutes with no on-chain effect — or after any restart — the same trigger fires again and the re-issue REPLACES the stale cert. Acceptors are epoch-monotone, so duplicates stay harmless; a gauge warning names the wedge instead of burning silently.
### Removed vs 0.12.0/0.12.1
- **R10 (bounded decided window + disk-served value-sync) is REMOVED.** It assumed a complete on-disk block log, but the staging nodes keep only block #1 on disk and value-sync the rest from peers — so R10's restore started the engine at height 0 (stuck). Restore/`start_height`/`decided`/value-sync are back to the proven v0.11 behavior. The memory/fast-restore win it aimed for returns later as its own release, redesigned to respect peer-sync nodes and tested against a sparse block log (not just a fresh devnet).
### Roll discipline (learned 2026-09-02)
- One voter at a time; confirm the node is actually **voting** (not merely `active`) before the next; gateway last; size the activation height against voter tips, never the public RPC edge.
### The fleet roll (2026-09-02) — DEPLOYED, and R9's production receipt
- Rolled val-1 → val-0 → val-2 → gateway, each verified voting before the next; the legacy `snapshot.bin` decode restored real v0.11 snapshots cleanly on every node (height-preferring loader picked the freshest image over a stale `snapshot2.bin` left by the aborted roll). **All four voters on v0.12.2, fee policy armed fleet-wide: `{micro: 100, from_height: 110000, burned: 0}`.**
- **R9 healed a validator with no human in the loop.** val-0 had burned its fresh epoch-4 tree to zero during the day (the same wedge as the morning incident, reproduced within ~10 hours on the un-fixed v0.11 binary). Rolled to v0.12.2, its first tick found the guard un-wedged, logged `R1.b tick: leaf budget under threshold … issuing rotation cert out-of-band`, pushed the epoch-5 cert to all three peers, and within a minute the node was signing again with zero refusals. Cadence recovered from a crawl to ~26 blocks/min as soon as its signer lived.

> **STATUS of the entries below:** [0.12.0] and [0.12.1] were the in-development work and its aborted first roll — kept for the record. They shipped R10, which v0.12.2 removes.

## [0.12.1] — 2026-09-02 — 🎯 activation height corrected (the gateway's height is not the chain's height)
- One-line release: `FEE_ACTIVATION_HEIGHT` 96,000 → **110,000**. The original constant was sized against the public RPC's height while the gateway was ~14k blocks behind the voters (post-restore catch-up — a lag class R10 itself eliminates going forward). The real chain had already passed 96,000 when the roll began; two upgraded voters were one tx-carrying block from a 2-2 state split. Caught before any transaction landed — the fleet was reverted to v0.11.0 within minutes (versioned on-disk binaries), no divergence occurred, and BFT safety guaranteed the worst case was a halt, never corruption. New operating law, now in the constant's doc comment: **activation heights are sized against voter tips, never an RPC edge.**

## [0.12.0] — 2026-09-02 — 💸 U4 + R9 + R10: fees are law, rotation can't wedge, memory is bounded
### ⚠ CONSENSUS-BEHAVIOR CHANGE — upgrade before height 110,000
- **U4 — flat protocol fee.** From activation height **110,000** on staging-1, every transaction envelope pays a flat **100 micro** in the test asset, charged to the sender and **burned**. The fee is debited BEFORE the payload executes (a full-balance sweep now honestly refuses: the tx's own spend sees the post-fee balance) and **refunded in full when a transaction refuses** — a refusal never moves money, exactly as before. New refusal: `insufficient balance for the protocol fee`. A `fees_burned` counter enters the state commitment only once nonzero, so upgraded and pre-upgrade nodes stay byte-identical until activation — a rolling fleet upgrade cannot fork early. `hk_chainInfo` now reports `fee.{micro,from_height,burned_micro}`. Honesty: this is an anti-spam floor and a mechanism rehearsal for testnet-1's real fee schedule — not a fee market; refusals are free by design (each attempt still burns the SENDER's one-time key material). U4.b (testnet-1): fee parameters move into genesis, like the vk pins.
- **R9 — rotation guards can no longer wedge a validator.** Production incident (2026-09-02): a validator burned its epoch tree to zero WITH the auto-rotation tick armed — a previously-issued cert that never committed left the "already issued / still pending" guards blocking forever. Fix: those guards now hold only while an issue is FRESH; after 10 minutes with no on-chain effect (or after any restart), the same trigger fires again and the re-issue REPLACES the stale cert. Acceptors are epoch-monotone, so duplicates stay harmless. A gauge warning now names the wedge instead of burning silently.
- **R10 — bounded memory, disk-served history.** The in-RAM decided map (full block history + hash-signature-heavy commit certificates, kept for value-sync serving) had become the fleet's ceiling: 5–7 GB per node at ~88k blocks, and a ~10-minute multi-GB restore. Now only the most recent **512 heights** stay in RAM (`HK_DECIDED_WINDOW`); anything older is served to value-sync straight from the disk block log via the same reconstruction the independent-observer receipt proved byte-identical. Restores no longer rehydrate history at all (it is decoded once for the search indexes and dropped): **gate measured RPC back in 1 second**. Steady-state memory becomes O(window), not O(height). Snapshots move to a versioned `snapshot2.bin` (the old `snapshot.bin` is read once, on upgrade, then superseded).
### The gate (devnet, 2026-09-02)
- hk-state 13/13 (new: fee burn/refund/activation/snapshot-roundtrip) · hk-node 17/17 (new: R9 re-arm) · live drill: burn counter 100→200 across exactly the two successful txs; sweep refused fee-aware (`have 249900, need 250000`); refused tx cost nothing · node restart with divergent fee config **refused to run on app_hash divergence** (the refuse-on-mismatch posture, now covering fee policy) · correct relaunch: RPC back in 1 s, search served from the rebuilt disk index, snapshot2 written.

## [0.11.2] — 2026-09-02 — 🔎 SEARCH EVERYTHING — the explorer answers, the wallet links
Non-consensus (RPC + UI only; wire untouched — only the public gateway needs this to serve search).
### Added
- **Node-local search indexes** (never in C(Σ) — anyone rebuilds them by replaying): txid → (block, index) and account → full history (as sender OR counterparty, with kind tags). Built on every commit AND on restart (replay path indexes via the shared apply path; rehydrated blocks are decoded and indexed explicitly), so search covers the whole chain after any restart. At current scale the indexes are a few thousand entries — in-memory by design; a dedicated indexer service is deliberately deferred to testnet-1 traffic levels.
- **Two RPCs:** `hk_getTx {txid}` → block height, in-block index, full summary (from the block log — answers forever, even after the receipt ring evicts), receipt if still cached; `hk_getAccountTxs {id, limit}` → newest-first history with kinds + total.
- **Explorer: search anything** — one box auto-detects block height / txid / account id / nullifier. Account view shows balance (with the honest "shielded holdings are invisible by design" note), ratchet index, and clickable history; tx view links to its block; every txid and sender in block details is clickable. **Deep links:** `#tx=…`, `#account=…`, `#block=…` — shareable, and the wallet uses them.
- **Wallet v0.11.2:** every drip and payment in the activity log now carries a **"view ↗" link** straight to its transaction in the explorer, and the account header gains "view on explorer ↗".

## [0.11.1] — 2026-09-02 — 🖥️ THE WALLET RELEASE — the front door behind a double-click
Non-consensus addition (new crate only; no protocol or wire changes — v0.11.0 nodes are unaffected).
### Added
- **HashKinetics Wallet for Windows** (`chain/crates/hk-wallet-gui` → `HashKinetics-Wallet.exe`): create or restore a post-quantum keychain, tap the faucet (auto-detects create-and-fund vs top-up), watch your balance, and pay any account id — against the live staging network, no CLI required. Native Rust (egui) — no browser runtime, no installer; one `cargo build --release -p hk-wallet-gui` produces the .exe.
- Same custody discipline as everything else in this repo: keys are generated locally and never leave the machine (the faucet only sees a commitment); the L-ratchet nonce is persisted BEFORE submit and rolled back only on a definitive on-chain refusal; the chain's nonce is adopted when a restored backup drifts. The wallet file is byte-compatible with the CLI's `account.json` — CLI and GUI share a wallet.
- v1 is the transparent journey (join → get funded → pay). Shielded operations via the public prover are the next wallet milestone.
### The gate (live staging chain, 2026-09-02)
- Wallet created → the second-ever public faucet drip → balance 0.100000 → a payment **signed at ratchet index 0**, receipt `ok`, publicly visible in the explorer (block 86,063, round 0, hash-based commit certificate).
### Honesty
- The distributed .exe is an **unsigned testnet build** — Windows SmartScreen will warn on first run. Verify the published sha256 instead of trusting the popup; code signing arrives with the production testnet.

## [0.11.0] — 2026-09-02 — 🚪 U1+U2: THE FRONT DOOR — anyone can join, get funded, and pay
The usage sprint opens. Until today, accounts were genesis-only: the chain was something you could *verify* but not *join*. v0.11.0 adds runtime account creation, a public faucet, and a self-custody CLI — a stranger goes from nothing to paying on-chain in under a minute.
### ⚠ CONSENSUS-BREAKING — upgrade before use
- A new transaction variant is appended to the wire format. Nodes **below v0.11.0 cannot decode any block containing an `AccountCreate`** and will refuse the batch. All validators and syncing nodes must upgrade BEFORE the first account creation commits; the staging-1 join kit now pins v0.11.0 as the minimum.
### Added
- **`Tx::AccountCreate { id, auth_commit, asset, amount }`** — permissionless, squat-proof runtime accounts: consensus enforces `id = H(DOM_ACCOUNT_ID ‖ auth_commit)`, so an identity can only be claimed by whoever holds the key material behind it, and no existing account can ever be overwritten. The sender funds the opening balance from its own (0 allowed); structural refusals run before money moves, so a failed create never debits. New refusals: `account already exists`, `account id does not match H(auth_commit)`. Explorer shows creations as `account_create`.
- **Self-custody account CLI** — `account-new` (32 bytes of OS entropy → keychain + derived id, refuses to overwrite key material), `account-info`, `account-balance`, `account-send`, `account-create` (sponsor anyone onto the chain from your own funds). The L-ratchet nonce follows the consensus signer's reserve-then-sign discipline: persisted BEFORE submit, rolled back (and persisted) only on a definitive on-chain refusal, and re-synced from the chain when the local file drifts — a crash can never re-sign a spent index.
- **`faucet-serve` — the public faucet** (`POST /drip {auth_commit}` creates+funds; `{account}` tops up; `GET /health`). Per-IP cooldown + global daily cap (X-Forwarded-For aware, failed drips refund the stamp); CORS-by-construction (every response carries the header, and the website calls it with no custom headers — a simple request needs no preflight). Until fees land, rate limits are the anti-spam floor.
- **hashkinetics.org/faucet** — three steps on one page: generate a keychain locally, paste the auth commit, spend. Keys never leave the user's machine; the faucet only ever sees a commitment.
### The gate (devnet, 2026-09-02 — the first complete user journey)
- `account-new` → id `00c6b30e…` · POST /drip → **AccountCreate committed** (txid `0d7a5f93…`) · balance $0.25 · the newcomer's OWN first signature (ratchet index 0) paid $0.05 back — receipt `ok: 1 event(s)` · balance $0.20. Squat, duplicate, and overdraft refusals covered by `u1_runtime_account_creation_faucet_flow` (hk-state 12/12, hk-node 16/16).
### Honesty
- Staging drips are deliberately tiny — the staging genesis carries a ~$50 total transparent supply. The production testnet (testnet-1) launches with a real faucet treasury (private seed) baked into genesis.

## [0.10.10] — 2026-09-01 — 🛡️ R8 + R1.b: a validator can nap without eating its keychain
Built for the external-validator era: strangers' nodes WILL go offline, fall behind, and wedge — ours only survived because we babysat them. Two shipped fixes make a parked validator boring instead of fatal.
### The gate (devnet, 2026-09-01 — the val-0 scenario, reproduced and cured)
- A voter was wedged below tip with value-sync disabled: it spent **2 leaves** (one round, before peer evidence arrived), then went silent — claim path confirmed live (`532` future-height votes recorded from all 3 distinct peers; core-consensus buffers them at exactly the instrumented ingress).
- Sync restored: it climbed **850 heights in ~70 s** while R8 suppressed every futile vote attempt en route (9× `R8: behind the network — abstaining … height=12 peer_tip=Some(830)`), landed **byte-identical** with the fleet (`app_hash c59d0effa184cb68` at h=862, 5 ms apart), and cast a live Prevote at **h=863** — the first possible height.
- The leaf ledger: the wedged voter finished at **69 used vs 2,358–2,566** on its peers — ~39 spent total (stuck round + live voting after rejoin), versus ~1,700 that pre-R8 catch-up voting would have burned. Forensics closed en route: rebroadcast re-sends stored votes (never re-signs, free), a truly-stuck driver is protocol-quiescent (~2 leaves per event), and the historical 32,768-leaf burn is now fully attributed to catch-up voting — the exact path R8 gates.
### Fixed
- **R8 — abstain-while-behind: futile votes no longer burn one-time leaves.** A validator wedged below the network tip used to cycle consensus rounds at its stale height — every nil prevote/precommit spending an irreplaceable LMS leaf (a fresh 32,768-leaf tree burned to ZERO in ~20 h of exactly this). New `Context::abstain_threshold()` (default `None` = upstream unchanged — the same pattern as `validator_set_at` and `placeholder_signature`): when corroborated peer evidence puts the network ≥ gap heights ahead, the driver skips all three leaf-spending arms (vote signing, proposal signing, value building). Abstaining is indistinguishable from being offline, which BFT already tolerates. The signal: future-height votes/proposals (already buffered by core-consensus) record per-validator height claims — **only current-set members count** (spam-proof), and the trigger is the **second-highest claim among distinct validators**, so one faulty validator can't fire it and a lone ahead-of-quorum node can't stall the majority. Default 20-height gap (`HK_ABSTAIN_GAP` overrides, `=0` disables). Ledgered residual: a same-height stall (quorum-loss halt) still spends leaves on live rounds — those rounds could actually commit, and the burn decays with round-timeout backoff.
- **R1.b — rotation threshold now evaluated on a tick, not only on commit.** The leaf-budget check lived in the commit path — a node that never commits (parked, minority partition: exactly the node whose leaves are draining) NEVER evaluated it, and burned to zero with the threshold armed. A 60 s tick (observers no-op) reads the live gauge, runs the SAME `rotation_due` (threshold clause only — unit-tested shape), and on due issues the root-signed cert **out-of-band**: self-enqueued into the foreign-cert queue AND pushed to every gossip peer via `hk_submitRotation` — the peer-carried revival path, self-served before exhaustion instead of human-driven after it. Epoch-monotone acceptors make duplicate issuance harmless by construction.
### Together
R8 preserves the leaves; R1.b spends the preserved leaves on a rotation cert while they still exist. The incident pattern (parked → burned → dead → human revival) becomes: parked → silent → self-rotated → rejoins. Binary: sha256 `2d6562912b90e6a319c126bf8b9c66bb97f03fd34f7f8b1b0e93c25c1adfe8a6`, 68,675,328 bytes.

## [0.10.9] — 2026-08-31 — 🍃 R5.4: SYNC SPENDS NO LEAVES — the join kit stops paying rent
### Found in production, same day (deploy receipts)
- **R3 CLOSED by measurement:** val-2 restarted on v0.10.9 and was back at tip, in lockstep with the gateway, in **under 4 minutes** (was: ~25 min rehydrate then trailing forever). R5.2's parallel apply cured deep-restart trailing as a side effect. Corollary observed: with 3/4 voters the chain slows to ~4 blk/min (zero timing margin) and self-heals at 4/4 — restarts stay one-at-a-time.
- **Join-kit gap: a node without `HK_PROVER_URL` wedges at the first proof-carrying shielded tx.** The SP1 verifier wires only when the env points at an hk-prove endpoint (vks fetched once, checked against genesis `vk_pins`); without it the chain keeps the RejectAll default — correct refusal posture for TRAFFIC, but a SYNCING node then rejects a canonical block's shield tx and diverges (`app_hash divergence`, observed at height 2,389 on the observer: gateway `ok: 1 event(s)` vs observer `rejected: pool proof rejected`). Fixed operationally (fleet nodes point at an internal vk-cache; public joiners use `https://prover.hashkinetics.org`) and documented in the join kit + onboarding. Real fix queued: ship the verifying keys IN the join kit so verification needs no live prover.
- **Leaf-freeze verified:** with R5.4 live the observer's signer `remaining` froze while height advanced — sync now spends zero leaves; the unwedge watchdog is dormant insurance.
### Fixed
- **Syncing no longer consumes one-time signature leaves.** Upstream malachite fabricates an internal `SignedProposal` for every synced value and signs it with the node's REAL key ("for now we keep all happy…", its own TODO admits) — harmless for Ed25519, but for stateful LMS/HSS that's one irreplaceable leaf per synced block: the first external observer spent its entire 32,768-leaf tree syncing and wedged. New `Context::placeholder_signature()` (default `None` = upstream behavior, same shape as R6's `validator_set_at`): `HkContext` returns an empty dummy for these internal-only, never-verified proposals — zero leaves spent, sync no longer depends on the local signer at all. The v0.10.8 unwedge watchdog becomes dormant insurance.

## [0.10.8] — 2026-08-31 — ⚡ R5.2: PARALLEL VERIFICATION — the join kit learns to outrun the chain
The observer-from-genesis receipt exposed the truth: catch-up collapsed to ~2 blocks/min against a chain adding ~27/min — a joiner that can never converge. Recon found the floor was never one thing:
### Fixed
- **Duplicate certificate verification eliminated.** Every SYNCED block verified its commit certificate twice — once on the sync path, again on the decide path, same certificate. `State.last_verified_certificate` (height, round, value-id) memoizes the sync-path pass; `decide` skips the re-verify on match. Live decides never set the memo and verify exactly as before. Half the hash-based signature work per synced block, ~15 lines.
- **One state-commitment walk per block, not two.** The post-apply `state_commitment()` (log line) and the next block's parent-hash check recomputed the SAME value — each an O(entire state, every nullifier ever) walk that GROWS with height (this, not constant-cost crypto, is why the observer's rate decayed). The post-apply hash is now cached and consumed by the next parent check; any error path drops the cache and recomputes honestly.
### Added
- **Batch signature verification hook** (`SigningProvider::verify_signed_votes_batch`, default `None` = serial fallback — same shape as R6's `validator_set_at`). The blanket `verify_commit_certificate` does one cheap ordered pass (duplicates, set membership, precommit reconstruction), then hands ALL LMS/HSS verifies to the provider's fast path. `HkSigningProvider` runs them on a dedicated **32 MiB-stack rayon pool** (`hk-consensus::par` — hbs-lms overflows default 2 MiB workers; capped at 8 threads). Boxed providers delegate, so the engine's `Box<dyn SigningProvider>` keeps the override.
- **Parallel envelope pre-verification.** A fat load-test-era block paid thousands of Lamport verifies serially INSIDE the state lock. The pure part (signing digest + Lamport over the tx's own fields) now runs `par_bools` outside the lock; `apply_block_verified` consumes the verdicts with check order IDENTICAL to the inline path (account → nonce → auth-commit → signature), so the success set is byte-for-byte unchanged whichever path a node takes. The key→account binding stays ordered.
- **Apply-cost telemetry** — `R5.2 apply-cost window` log every 100 blocks: pre/agg/apply/hash/save/snap ms. The 450 ms/block floor was an inference; now every node measures its own.
- **`HK_SNAPSHOT_EVERY`** env (default 16): the full-image snapshot + fsync grows with state and taxes catch-up; a grinding observer can widen the cadence (crash cost = replaying a few more blocks).
### Deliberately unchanged
- Engine-level batch pre-verify of whole sync batches (20-way across certificates) parked as **R5.2b** — measure first; the per-block wins may already clear the ~27 blk/min convergence bar.
### Proven in production, same day
- **Observer hot-swapped to v0.10.8** (e2-standard-8): sustained **71 blocks/min** measured over 2 minutes (was ~2 blk/min — 35×), net +44 blk/min against the live chain. Telemetry window at the early era: `pre=0 agg=0 apply=0 hash=0 save_ms≈3.2/blk` — after R5.2 the measured bottleneck is the per-block fsync, not crypto.
- **New failure mode found and fixed operationally: observer signer exhaustion.** A syncing non-validator SELF-SIGNS each synced proposal to drive the state machine (~1 LMS leaf/block; wedge-loops burn far more) — the original observer silently spent its ENTIRE 32,768-leaf tree (state file read `used = 0x8000` exactly), and every later sync value died as `Unexpected resume: Continue, expected Resume::SignedProposal`. Non-validators have no on-chain revival (their root isn't in the set), but their self-signatures are consumed only locally — so the remedy is: stop, rotate the spent `consensus_state.bin` aside, start (fresh tree). An **unwedge watchdog** (systemd timer, 2-min cadence) now does this automatically; the tree covers ~30k blocks per cycle. Proper fix parked as **R5.4: a syncing non-validator should not spend hash-based leaves at all** (accept sync values without self-signing, and let a failed SignProposal degrade gracefully instead of erroring the message).

## [0.10.7] — 2026-08-30 — 🧠 R6: PER-HEIGHT VALIDATOR-SET VERIFICATION — the chain remembers who signed what
### Added
- **Commit certificates now verify against the validator set AS OF their height** (the set that actually signed them), not the current set. `Context::validator_set_at(height)` (default `None` = old behavior) + the engine's `VerifyCommitCertificate` handler swaps in the historical set keyed by `certificate.height` — one interception point fixes both the sync and decide paths, no wire changes. `HkContext` answers from a shared **set history** (`(effective_from_height, set)`), seeded at genesis, reseeded at snapshot restore, appended on every committed rotation (live and replay).
- **`decide.rs` de-panicked:** a certificate failing verification used to `assert!` → kill the consensus actor → poison the shared validator mutex → half-dead node with a live RPC (the exact production corpse pose, twice). It now returns `InvalidCommitCertificate` with an error log; all `validators.lock()` sites recover poisoned guards.
### Proven in production, same day
- **val-0's THIRD revival:** E3 rotation cert issued offline + carried by a peer (`hk_submitRotation`), store transplant, then **organic sync across live rotation boundaries (E44/E7/E8) → adopted the fresh E3 tree mid-sync → voting. True 4/4 at 50,443+.**
- **Observer-from-genesis receipt (in progress):** a brand-new VM joined via `networks/staging-1` (public seed DNS, published fingerprint verified: `557f2ea6c…`), reports `chain_id: hashkinetics-1-557f2ea6`, and is re-verifying the entire chain from block 1 — every rotation boundary, every STARK.
### Operational findings (ledgered)
- Transplant tars must pack `snapshot.bin` FIRST (blocks-then-snapshot ordering left the snapshot ahead of the blocks tail → engine/chain height skew → wedge).
- Deep catch-up apply on e2-standard-2 cannot outrun the live chain (R5.2/R3 territory); interim cure = **thin teleport** (snapshot + newest ~400 blocks → 2-minute restore, lands at tip). R3 (resume-at-tip) is the real fix.

## [0.10.6] — 2026-08-30 — 🪪 THE IDENTITY RELEASE — chain_id bound to genesis; islands refused
### Added
- **`chain_id` is derived from the genesis digest** (`hashkinetics-1-557f2ea6` for staging-1) instead of a hard-coded label — a node on a different genesis visibly reports a different chain.
- **`hk_chainInfo` returns `genesis_digest`** — the full SHA-256 fingerprint, machine-checkable against `sha256sum genesis.json` and the public RPC.
- **Nodes refuse to peer across genesis:** each node advertises its digest in the libp2p identify handshake (`agent_version`); consensus-protocol peers with a mismatched digest are disconnected. Pre-gate peers are graced, so rolling upgrades cannot partition. `protocol_version` untouched.
- **R5 published:** continuation-driven value-sync (re-request at every capacity-freeing exit + stale-pending expiry; batch 5→20) — ~an order of magnitude on deep backlogs.
### Fixed
- Published staging-1 genesis fingerprint had a 3-nibble hand-transcription typo; now matches `sha256sum genesis.json` exactly.

## [0.10.5] — 2026-08-28 — 🔁 R-SERIES: SURVIVABLE KEY ROTATION — keys that retire themselves
### Added
- **R1 — threshold rotation:** a validator whose HSS leaf budget crosses <20% (6,553 of 32,768) automatically issues a root-signed rotation cert that rides its next proposal. First unattended production rotations observed overnight (epoch badges climbing on the public explorer).
- **R2 — survivable exhaustion + revival tooling:** an exhausted signer returns `Err` (engine swallows; node keeps syncing/serving) instead of halting; `hk-node issue-rotation` CLI + `hk_submitRotation` RPC let ANY peer carry a dead validator's revival cert (used in production for every revival since).
- **R4 — leaf gauge:** `hk_chainInfo.signer {epoch, remaining, capacity}` + tiered budget logs — operators watch the fuse.
- **WS-F closed:** `adopt_epoch_signer` — post-restore, the live signer rebuilds at the chain's epoch for our entry; per-epoch state files resume their own used-leaf counters (leaf reuse impossible across restarts).
- `networks/staging-1/` join kit (pinned genesis + PEERS + README) · v0.10.5 released immutable on GitHub.
### The incident that earned it (2026-08-27, staging)
- A validator burned all 32,768 one-time signatures and the chain **halted rather than reuse a leaf** — hash-based consensus keeping its core promise the hard way. Recovery was the designed path (offline root-signed cert carried by a peer), not a patch. Full postmortem in the honesty ledger.

## [0.10.4] — 2026-08-27 — ⚡ C2: THE THROUGHPUT LIFTS + GCP STAGING TESTNET LIVE
### Added
- **C2.1–C2.5:** admission pre-checks on submit · indexed mempool prune · tx gossip between mempools · block cap 256→1024 — **storm re-measure: 274 tx/s sustained** (2.2× the 0.10.3 baseline) · full test suite + demo suite green.
- **Public staging testnet on GCP:** 4× e2-standard-2 (3 validators + gateway), nginx-fronted public RPC (`rpc.hashkinetics.org`), explorer wired to it, seed DNS (`seed.hashkinetics.org`), 2s cadence.
- **C1p2.a measured (RTX 5090):** aggregation curve N=4…256 — `T_agg(N) = 2.66 s + 0.2882 s·N`, proof size CONSTANT (1,242 KB); 256 shielded spends → ONE proof in 75.4 s.

---

*Public log begins at the staging-testnet era (v0.10.4). Earlier internal build history is not part of the open-source release.*
