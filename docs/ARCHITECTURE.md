# Architecture map — plan → code

> **Status 2026-09-14:** tree at hk-node v0.19.3 (consensus rules v0.19.0 since height 190,000); 26 JSON-RPC methods (`docs/RPC.md`); the rows added since the 2026-08-15 map are marked with their version. Release history: `CHANGELOG.md`.

Plan: `../HASHKINETICS-IMPLEMENTATION-PLAN.md` (sections referenced below).

| Plan | Code home | Vendored inputs |
|---|---|---|
| §3.1 Two-hash doctrine | `chain/crates/hk-crypto` (`hash.rs`: SHAKE-256 external; Poseidon2/RPO in-circuit later) | `rustcrypto-hashes`, `plonky3`, `miden-crypto` |
| §3.2 Signature stack | `hk-crypto`: `hashsig.rs` (**LMS/HSS over SHAKE-256, real + KAT-verified, 0.9**) · `traits.rs` · Lamport L-ratchet (`lamport.rs`, account auth) · SLH-DSA/WOTS adapters feature-gated | `hbs-lms-rust` (LMS, wired), `fips205-rust` (SLH-DSA root, live since 0.9.2), `xmss-reference-c` + `lms-hash-sigs-c` (KATs), the vendored `slhdsa-*` adapters |
| §3.3 Consensus vote signing | `hk-consensus` provider — **LIVE since 0.9.0** (design record: `docs/HASHSIG-CONSENSUS-SWAP.md`); rotation under SLH-DSA roots since 0.9.3, R-series hardening 0.10.5→0.13.0 | `hashsig` primitive above; `malachite` SigningScheme seam |
| §6 QHT channels + §12 agent GTM | **`hk-facilitator` (RAG-as-a-merchant, 0.8): PayWord-metered paid search settled on-chain** | `agentic/turbovec` (drop-in index), `x402` (facilitator pattern) |
| §3.3 Leaf-index=nonce (enforced in `hk-state` accounts) · equivocation slashing (planned, WS8) | IETF draft-wiggers patterns (research 03) |
| §3.4 ML-KEM confidentiality boundary | `hk-crypto` (feature `mlkem`, live since 0.9.7 — stealth addresses) | `rustcrypto-kems` |
| §4 Shielded pool / CVA | `hk-state::pool` (consensus state) + `hk-wallet` (client) + `zkvm-bakeoff/circuit` (`hk-spend-circuit`, the shared statement) — live since 0.9.6; disclosure 0.9.8; aggregation 0.9.9 | `penumbra` (tct, view service), `librustzcash` (note plumbing), zkVM from bake-off |
| §5 MandateTree v2 | **`chain/crates/hk-mandate` (drip math implemented + tested)** | `docs/carried-specs/MANDATETREE-SPEC.md`, `ap2` (mandate mapping) |
| §6 QHT channels | `hk-crypto/payword.rs` (implemented) + `hk-channels` (P0/P1) | `docs/carried-specs/HYPERTREE-CHANNELS-SPEC.md`, `x402` |
| §7 Chain / consensus | **`hk-consensus` (HkContext, devnet-proven) + `hk-node` (full node: codec/streaming/loop/testnet-gen; LIVE 4-validator devnet 2026-08-15)** | `malachite` (vendored engine), the earlier `spike-engine-adapter` (shape reference) |
| §7.4 Native objects | `hk-primitives` (types) → `hk-state` modules | — |
| Usage layer (0.11+) | `hk-node/src/account.rs` (self-custody accounts, `Tx::AccountCreate`), `faucet.rs` (public drip service), `rpc.rs` (26 JSON-RPC methods as of v0.19.3 — `docs/RPC.md`), `store.rs` + `state.rs` (block log, snapshots, R10 v2 disk-served history), `hk-wallet-gui` (Windows wallet, fee-aware + shielded) | — |
| Fees (0.12.2 / 0.13.0) | `hk-state` (charge → refund-on-refusal → burn, `fees_burned` in C(Σ)); genesis `chain.fee` authoritative (`docs/FEES.md`) | — |
| Validator-set changes (0.14.0, V1) | `hk-consensus/src/setchange.rs` (`SetChangeCert`: supermajority of current seats' SLH-DSA roots, chain-id + window bound); `hk-node/src/state.rs` (`pending_set_changes`, apply on commit + replay, HK-R6 history); batch wire v2 (`HK-BLK-2`) only when a block carries one; CLI `set-change propose|approve|assemble`; `hk_submitSetChange` (`docs/V1-VALIDATOR-SET-CHANGES.md`) | — |
| Issued assets (0.15.0, X1) | `hk-state/src/assets.rs` (registry, `H(issuer ‖ symbol)` ids, policy, commitment bytes) + `lib.rs` (`asset_gate` on every balance move, `AssetRegister/Mint/Burn/Freeze/Pause`, conservation I5′, snapshot v3); `hk-node` `store.rs` (`snapshot3.bin`), `rpc.rs` (`hk_getAsset/hk_getAssets`, balances by asset), `account.rs` (`asset …` CLI), `genesis-build --asset/--fee-asset` (`docs/X1-ISSUED-ASSETS.md`) | — |
| Wallet core + shells (P6.2, 2026-09-13) | `chain/crates/hk-wallet-core` (the mobile/desktop core, UniFFI bindings; v0.2.0 — assets) · `hk-wallet-gui` (the desktop wallet shell over the core → `HashKinetics-Wallet.exe` v0.15.0) · `android/` (the Android app over the core, CI-signed; v0.3.0) — `docs/WALLET-GUIDE.md` | — |
| Bridge (B1, live 2026-09-09) | `hk-node/src/attest.rs` + `eth.rs` (`attest-serve`: Sepolia watcher, mint / unlock submitters, hand-written Ethereum client) + `bridge/contracts` (Foundry: `HKVault`, `HKWrapped`, `AttestorSet`, `DailyLimit`) — `docs/BRIDGE-GUIDE.md` | `forge-std`, `openzeppelin-contracts` (`bridge/contracts/lib`) |
| Explorer | `explorer/index.html` (single file, zero build, live over the public RPC) | — |
| Networking patch (0.19.3, N2) | `vendor/external/rust-libp2p-gossipsub` — libp2p-gossipsub 0.49.5 plus the re-announce patch (a node re-announces its topic subscriptions on every additional connection to a peer and on close-with-remaining); `hk_getPeers` `reconnects` / `last_connection_secs` — `docs/INCIDENTS.md` #12 | vendored crate |
| §9 zkVM bake-off (gate G2 ✅ 1.24 s) | `zkvm-bakeoff/` (RESULTS.md; SP1 chosen, prover service `sp1/script/src/bin/serve.rs`) | `sp1`, `openvm`, `miden-vm`, (`risc0` via .bat) |

Build reality: no Rust toolchain in the Cowork sandbox — **Yadu's machine (ASUS-SERVER, WSL) runs `cargo build`**; the GCP fleet is rolled from there. Current status (2026-09-14, hk-node v0.19.3 — testnet-1 live since 2026-09-02, eleven seats; the paragraph below is the 2026-09-04 narrative plus what shipped since): **hash-based BFT consensus live since 0.9** (LMS/HSS over SHAKE-256 under an SLH-DSA root — the Ed25519/empty-blocks day-one note below is retired); shielded pool + aggregation + disclosure live (P2); durable node + staging testnet + R-series rotation production-proven (0.10.x); the usage sprint shipped runtime accounts, a public faucet, a Windows wallet, and universal explorer search (0.11.x); flat protocol fees + rotation re-arm (0.12.2); then **testnet-1** — a ceremony genesis with the fee policy and the faucet treasury bound into it, fresh validator keys, R10 v2 bounded memory with disk-served history, and the fee-aware + shielded Windows wallet (0.13.0); node hardening for public exposure + per-seat rotation jitter (0.13.2); **validator-set changes on the running chain** + K5 refuse-to-start-unverified (0.14.0); **issued assets** — registry, issuer mint/burn/freeze/pause, per-asset supply in C(Σ), the floor for a stablecoin issuer (0.15.0). Since then (`CHANGELOG.md`): K6 vk pins in the kit (0.15.1) · N1 live peer table (0.15.2) · S+K block-log segments + sealed key files (0.16.0) · H3 paged pool feed (0.16.1) · R11 verify-only SP1 client (0.17.0) · G1 bootstrap weights at height 110,000 (0.18.1) · R15 fast consensus signing (0.18.2) · **P6 one shielded pool per asset** at height 190,000 (0.19.0 — the consensus minimum) · B1 bridge + wallet account binding (0.19.1) · faucet `--drip-asset` (0.19.2) · N2 gossipsub re-announce (0.19.3). The four founding seats run on GCP on `hashkinetics-1-4e4ea68d` (staging-1 retired at 107,182, archived); seven external seats (#5–#11) were admitted on the running chain between 2026-09-05 and 2026-09-12. Historical day-one note (2026-08-15): 33/33 tests green, 4-validator devnet via `chain/devnet.ps1`, stock Ed25519 votes and empty blocks — all since superseded. Web demo mirrors of hk-mandate/payword live in `vercel/lib/` — keep byte-compatible when protocol changes.
