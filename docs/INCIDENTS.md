# Incidents — the published record

**Every incident on a HashKinetics public network, what it cost, what it proved, and what shipped because of it.** Appended, never edited; severity per `validators/OPERATIONS.md` (SEV-1 safety · SEV-2 liveness · SEV-3 single node · SEV-4 cosmetic). The public changelog carries the receipts; this page is the index.

| # | Date (UTC) | Network | Sev | What happened | Root cause | Cost | What shipped |
|---|---|---|---|---|---|---|---|
| 1 | 2026-08-27/28 | staging-1 | **SEV-2** | **THE HALT.** val-0 signed with its last one-time leaf at height **10,848**; the chain stopped deciding rather than reuse a leaf. ~6 h to recovery. | No rotation trigger was armed — the leaf budget (32,768 ≈ 10.9k heights at ~3 sigs/height) ran out on schedule. | ~6 h of no blocks; zero safety impact; zero funds. | v0.10.5 R-series: automatic threshold rotation (R1), survivable exhaustion + peer-carried revival certs (R2, `issue-rotation` + `hk_submitRotation`), leaf gauge (R4), WS-F restart-after-rotation resume. |
| 2 | 2026-08-29 → 08-31 | staging-1 | SEV-3 ×3 | **Three full-exhaustion revivals of val-0** (E1, E2, E3) — each time a root-signed cert issued offline and carried by a peer revived the seat; E3 synced across live rotation boundaries on the way home. | Residual leaf burn on futile catch-up rounds; a syncing node self-signed synced proposals. | Operator time; no blocks missed (3/4 quorum). | R6 per-height set verification (v0.10.7), R5.2 parallel verify 2→71 blk/min (v0.10.8), R5.4 zero-leaf sync (v0.10.9), R8 abstain-while-behind + R1.b tick threshold (v0.10.10). |
| 3 | 2026-08-30 | staging-1 | SEV-3 | **The observer that ate its tree.** The first external-style observer node spent its entire 32,768-leaf tree syncing (state file read `used = 0x8000`). | Sync path signed each synced proposal to drive the state machine. | One observer tree. | v0.10.9: syncing spends zero leaves; unwedge watchdog (dormant insurance). |
| 4 | 2026-09-02 (morning) | staging-1 | SEV-3 | **val-0 wedged by a stale rotation issue**: a cert issued but never committed left the rotation guards blocking, and the seat burned its fresh tree to zero while waiting. Reproduced within ~10 h on the same binary. | Guards held forever after an issue that never landed. | One epoch-4 tree; cadence crawled on 3/4. | R9 stale-issue re-arm (v0.12.2): guards hold 10 min or until restart, re-issue replaces. **Production receipt the same evening:** the rolled node healed itself with no human in the loop (epoch 5). |
| 5 | 2026-09-02 | staging-1 | SEV-3 (aborted roll) | **v0.12.0/0.12.1 roll aborted.** The first R10 (bounded memory) derived the engine's start height from the on-disk block log; on a validator that kept only block #1 on disk and value-synced the rest, restore started the engine at height 0. | Design assumed a complete block log. | One node's restart; rolled back on the spot. | v0.12.2 shipped fees + R9 without R10; R10 v2 (v0.13.0) takes the start height from the chain, advertises only the gap-free suffix, and was gated by four restore shapes in `chain/rehearsal.sh`. |
| 6 | 2026-09-02 | fleet | SEV-4 | **Memory.** ~6.7 GB RSS per node on 8 GB VMs right after launch, with a 70-block decided map. | The SP1 verifier client's retained init footprint, not history. | Headroom; no outage. | Ledgered as R11 (verify-only client). Operators are told 8 GB minimum / 16 GB comfortable. |
| 7 | 2026-09-02 | ceremony | SEV-4 (procedure deviation) | The VM service accounts are read-only on object storage; the genesis could not be distributed via a relay bucket and the staging-1 archive could not be pushed by the VM identity. | Scope set at VM creation; changing it needs a VM stop. | None (genesis went host-to-host over ssh as base64, byte-identical; the archive went up with a one-hour operator token over ssh stdin). | Rule 5 of the ceremony doc; the next ceremony plans the token route or widens the scope first. |
| 8 | 2026-09-02 | rehearsal harness | SEV-4 | Two false FAILs and a 25-minute hang in `chain/rehearsal.sh`, none in the node: a log-field parser that could not see through ANSI colour, and a node launcher that held the pipeline's stdout. | Harness bugs. | Operator time. | Both fixed the same night; gate GREEN 34/34 on the released binary. |

## What the record says

- **Safety has never been violated.** Every incident is a liveness or single-node event; no divergent commit, no funds moved wrongly, no leaf reused. The one time the protocol had to choose between liveness and key safety (#1) it chose key safety, by design.
- **Every incident shipped a mechanism, not a patch:** the R-series exists because of #1–#4; R10 v2 exists because of #5; the rehearsal gate exists because of #5 and #8.
- **The first testnet-1 rotation (2026-09-03, heights 11,584–11,595) was not an incident** — all four seats crossed their thresholds and rotated unattended, zero blocks missed. It did expose that seats keyed together rotate together (within 11 blocks), which is ledgered as R12 (threshold jitter) in `P3.2-IMPLEMENTATION-PLAN.md`.

## How to report one

Security-relevant findings: privately, `security@hashkinetics.org` (`SECURITY.md`, 90-day coordinated disclosure). Operational incidents on your own node: the Discord `#testnet` channel with the runbook section you tried (`RUNBOOK-DEVNET.md` §4/§7). Anything that touches the soak clock is post-mortemed here.
